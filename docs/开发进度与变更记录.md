# Agent 测试平台开发进度与变更记录

本文件是项目开发状态的唯一人工可读台账。所有 AI Agent 和开发人员在开始和完成任务时都必须更新本文件。

更新规则见根目录 `AGENTS.md`。

---

## 1. 项目状态

| 项目项 | 当前状态 |
|---|---|
| 当前阶段 | 生产真实性与质量收敛 |
| 当前里程碑 | 清除生产路径 Mock、占位、假成功和运行时硬编码 |
| 总体状态 | 待真实环境验证 |
| 最后更新 | 2026-07-03（TASK-20260703-001 待验证） |
| 当前代码状态 | Agent 对话生成恢复/取消/SSE 游标与 Codex 内联时间线已实现；未登录死锁、停止部分正文与重连游标问题已修复；变更域测试、Lint、类型、构建与 API drift ✅ |
| 当前阻塞 | 真实 Temporal/Model Runner E2E 需要可用供应商配置；内置浏览器到达对话路由后只读检查持续超时，视觉 QA 尚未完成 |

---

## 2. 里程碑

| 里程碑 | 目标 | 状态 | 完成度 |
|---|---|---|---:|
| M0 开发准备 | PRD、架构、开发规范和实施计划 | 已完成 | 100% |
| M1 平台基础 | Monorepo、登录、用户、项目和权限 | 已完成 | 100% |
| M2 测试资产 | Agent、数据集、用例和测试计划 | 已完成 | 100% |
| M3 执行闭环 | Temporal、API Runner、运行与 Trace | 已完成 | 100% |
| M4 画布测试 | Canvas 插件、结构断言和多模态评测 | 已完成 | 100% |
| M5 浏览器测试 | Playwright 与 Browser Harness | 已完成 | 100% |
| M6 安全与门禁 | Promptfoo、安全策略和 CI/CD | 已完成 | 100% |

完成度是进度参考，不能代替验收标准。

---

## 3. 当前进行中

### TASK-20260703-001：Agent 对话 Codex 式全链路体验优化

- 状态：待验证
- 开始日期：2026-07-03
- 负责人：AI
- 分支：`codex/feature/agent-chat-codex-experience`
- 关联需求：PRD 4.1、5.1、6.1、6.6、8.8、15.2
- 关联架构：技术架构 2.4、4.2、6、7、8、14；现有 Temporal Model Runner 与 Test Agent 模块
- 目标：实现可恢复、可取消、无首包竞态的 Agent 对话链路，并采用 ChatGPT 正文风格与 Codex 内联过程时间线。
- 计划修改：Test Agent 会话/事件/生成状态 API、Temporal 取消链路、Web 对话状态机与时间线组件、迁移、测试和文档。
- 验收方式：前后端单元与契约测试、数据库迁移与项目隔离测试、SSE 重连/恢复/取消测试、浏览器关键交互检查、全量质量门禁。
- 设计规格：`docs/superpowers/specs/2026-07-03-agent-chat-codex-experience-design.md`
- 实现摘要：
  - 新增 `ChatGeneration` 状态机与 `0015` 迁移，记录稳定 Workflow ID、部分内容和终止状态。
  - 会话详情返回持久化 timeline、event cursor 和 active generation；SSE 先发 `stream.ready` 并按 cursor 补发。
  - 停止生成改为后端取消接口，Temporal 使用稳定 Workflow ID，Worker 取消时返回已累积内容。
  - 工具事件关联 generation；等待确认可刷新恢复，批准/拒绝后恢复同一轮并写入最终消息。
  - Web 使用服务端有序时间线，采用 ChatGPT 式正文与 Codex 式思考摘要/工具步骤；移除 800ms 完成猜测和消息数装饰进度条。
  - 修复未登录时禁用项目查询保持 pending 导致的永久加载，并将首页/工作台跳转移出 React 渲染阶段。
  - 补齐 generation 生命周期 SSE 订阅；`message.completed` 在 POST 中断后提交并去重部分正文；同步实时正文引用供停止操作使用。
  - 浏览器自动重连时优先采用 `Last-Event-ID`，避免初始 `after` 查询游标造成旧事件重复回放。
- 主要变更：
  - `apps/control-api/migrations/versions/0015_test_agent_chat_generations.py`
  - `apps/control-api/src/agenttest/modules/test_agent/**`
  - `apps/control-api/src/agenttest/modules/model_configs/**`
  - `workers/model-runner/src/agenttest_model_runner/activities.py`
  - `apps/web/src/features/test-agent/**`
  - `docs/api/openapi.json`、`packages/generated-api-client/src/client/**`
- 数据库/API/配置变化：
  - 新增 `test_agent_chat_generations`，`test_agent_events` 新增可空 `generation_id` 复合外键与索引。
  - `ChatRequest` 新增可选 `generation_id`；SSE 新增 `after` cursor 与 `stream.ready`；新增 generation cancel API。
- 验证记录：
  - 定向 Control API / Model Runner：63 passed。
  - Test Agent + 迁移：39 passed、1 skipped（缺少隔离 PostgreSQL）。
  - Web Test Agent：6 passed；TypeScript、定向 ESLint、生产构建通过。
  - 空 SQLite 从 base 升级至 `0015`，generation 表、事件复合外键和版本号检查通过。
  - OpenAPI/生成客户端：类型、测试与 drift 检查通过。
  - 全仓后端：597 passed、6 skipped、2 个既有架构失败。
  - 本轮 Web Agent/导航：11 passed；Prettier、TypeScript、生产构建通过。
  - 本轮后端 SSE 重连契约：2 passed；变更文件 Ruff format/check 通过。
  - 浏览器：401 后可从 `/projects` 正确跳转登录；本地 QA 账号登录成功并到达 `/projects/{id}/test-agent`。
- 未验证与风险：
  - 未连接真实 Temporal、Model Runner 和供应商模型验证取消后的费用停止与部分内容回传。
  - `/projects` 永久加载已修复；内置浏览器到达对话页后截图/只读 DOM 检查持续超时且无控制台错误，视觉与响应式 QA 仍未完成。
  - 全量门禁仍受既有 34 个格式文件、6 个 Web 账户/主题测试、PydanticAI mypy 与 2 个架构失败阻塞。
- 下一步：定位内置浏览器对话页检查超时并完成视觉 QA；在真实执行栈验证取消后将任务改为已完成。

### TASK-20260702-004：浏览器实例注册表前后端完整实现

- 状态：已完成
- 完成日期：2026-07-02
- 负责人：AI
- 分支：`codex/feature/codex-browser-integration`
- 关联需求：PRD M5 浏览器测试扩展（浏览器实例复用）
- 关联架构：ADR-0005 浏览器双引擎
- 实现摘要：
  - 后端：新增 `browser_profiles` 模块（`apps/control-api/src/agenttest/modules/browser_profiles/`）
  - 后端 API：`GET/POST /api/v1/projects/{id}/browser-profiles`、`GET/PATCH/DELETE .../browser-profiles/{pid}`
  - 存储：与插件层 profile_registry.py 共享同一套 `~/.agenttest/browser-profiles/{project}.json` 文件
  - 前端：新增 `BrowserProfileListScreen` 页面，含列表/创建/编辑/删除 UI
  - 前端路由：`/projects/[projectId]/browser-profiles`
  - 导航：侧边栏新增"浏览器实例"入口（Globe 图标）
  - 测试计划集成：`TestPlanConfig` + `TestPlanConfigRequest` 新增 `browser_profile_id` 字段
  - 测试计划对话框：执行配置步骤新增浏览器实例下拉选择
  - Worker：workflow.py 新增 `execution_policy.get("browser_profile_id")` fallback
  - OpenAPI + TypeScript Client 重新生成
- 变更文件：
  - `apps/control-api/src/agenttest/modules/browser_profiles/`（新增 3 文件）
  - `apps/control-api/src/agenttest/bootstrap/app.py`（注册 browser profile 路由）
  - `apps/control-api/src/agenttest/modules/test_plans/api/schemas.py`（新增 browser_profile_id 字段）
  - `apps/control-api/src/agenttest/modules/test_plans/domain/value_objects.py`（新增 browser_profile_id 字段）
  - `workers/api-runner/src/agenttest_api_runner/workflow.py`（fallback plan config）
  - `apps/web/src/features/browser-profiles/`（新增 3 文件）
  - `apps/web/src/app/(platform)/projects/[projectId]/browser-profiles/page.tsx`（新增）
  - `apps/web/src/components/layout/app-shell.tsx`（新增导航链接）
  - `apps/web/src/features/test-plans/test-plan-version-dialog.tsx`（集成 profile 选择）
  - `docs/api/openapi.json`（重新导出）
  - `packages/generated-api-client/`（重新生成）
- 数据库/API/配置变化：
  - 新增 5 个 REST API 端点（浏览器实例 CRUD）
  - `TestPlanConfigRequest` 新增 `browser_profile_id` 可选字段
- 验证记录：
  - `ruff check`：通过
  - `pytest apps/control-api/tests/`：572 passed, 5 skipped（不含既有迁移失败）
  - `pytest workers/api-runner/tests/`：53 passed
  - `pnpm typecheck`：通过
  - `pnpm build`：通过
- 遗留问题：
  - `test_migrations.py` 既有失败（0013→0014 版本号不同步，与本次无关）
  - 浏览器实例的"登录"操作（启动 Chrome → 人工登录 → 保存 storageState）需要在 Worker 环境验证
- 下一步：在真实 Worker 环境中启动浏览器实例、执行登录、验证 storageState 持久化

### TASK-20260702-003：Codex 浏览器生命周期管理与登录态复用

- 状态：已完成
- 完成日期：2026-07-02
- 负责人：AI
- 分支：`codex/feature/codex-browser-integration`
- 关联需求：PRD M5 浏览器测试扩展
- 关联架构：ADR-0005 浏览器双引擎
- 实现摘要：
  - 插件层新增 BrowserMode 枚举（ephemeral/persistent）、StorageStateConfig 数据类
  - codex_invoker.py：持久 Chrome 进程管理（ensure/health-check/shutdown）、storageState 读写（load/save/TTL 过期）
  - adapter.py：持久模式自动拉起 Chrome + CDP 连接、storageState 解析透传
  - manifest.json：v1.0.0→v1.1.0，新增 browser_mode/cdp_port/storage_state 配置项
  - Worker 层：codex_browser_activity.py 和 workflow.py 新增 browser_mode/storage_state_key/credentials 字段透传
- 变更文件：
  - `plugins/codex-browser/src/agenttest_plugin_codex/contracts.py`
  - `plugins/codex-browser/src/agenttest_plugin_codex/codex_invoker.py`
  - `plugins/codex-browser/src/agenttest_plugin_codex/adapter.py`
  - `plugins/codex-browser/manifest.json`
  - `workers/api-runner/src/agenttest_api_runner/codex_browser_activity.py`
  - `workers/api-runner/src/agenttest_api_runner/workflow.py`
- 数据库/API/配置变化：无
- 验证记录：
  - `ruff check`：通过
  - `mypy`：已有问题不变（plugin import 未安装、int() 参数类型）
  - `pytest workers/api-runner/tests/`：53 passed
  - `scripts/check_architecture.py`：已有问题不变（platform.py 跨模块导入）
- 遗留问题：
  - CDP 连接实际验证需要 Worker 环境安装 Codex CLI + Chromium
  - storageState 自动刷新逻辑需要在 Codex CLI MCP 层实现 Playwright browserContext.storageState() 采集
- 下一步：合并至 main、在真实 Worker 环境中验证持久模式和 storageState 复用

### TASK-20260702-002：Codex CLI + Google Chrome 浏览器探索集成

- 状态：已完成
- 完成日期：2026-07-02
- 负责人：AI
- 分支：`codex/feature/codex-browser-integration`
- 关联需求：PRD M5 浏览器测试扩展
- 关联架构：ADR-0005 浏览器双引擎、ADR-0006 Plugin SDK
- 实现摘要：
  - 插件层：`plugins/codex-browser/` — manifest + adapter + invoker + contracts（6 文件）
  - Worker 层：`codex_browser_activity.py` Temporal Activity + `workflow.py` codex_explore 分发
  - 报告层：`reports.py` Allure JSON + HTML 报告生成
  - 前端层：`api.ts` CodexExploreResult 类型 + `context-panel.tsx` CodexResultCard 组件
- 变更文件：
  - `plugins/codex-browser/`（新增 6 文件）
  - `workers/api-runner/src/agenttest_api_runner/codex_browser_activity.py`（新增）
  - `workers/api-runner/src/agenttest_api_runner/workflow.py`（codex_explore 分发）
  - `workers/api-runner/src/agenttest_api_runner/reports.py`（Allure 扩展）
  - `workers/api-runner/tests/test_reports.py`（测试适配）
  - `apps/web/src/features/test-agent/api.ts`（CodexExploreResult 类型）
  - `apps/web/src/features/test-agent/context-panel.tsx`（CodexResultCard）
- 数据库/API/配置变化：无
- 验证记录：
  - `ruff check`：通过
  - `mypy`：通过
  - `pytest workers/api-runner/tests/`：53 passed
  - `pytest apps/control-api/tests/`：124 passed + 5 skipped（排除预存 arch/迁移失败）
- 遗留问题：
  - Codex CLI 需要在 Worker 容器中安装验证
  - Allure CLI 需要在 Worker 镜像中预装
  - 前后端完整对话链路（SSE → 前端渲染）待环境就绪后验证
- 下一步：合并至 main、在真实 Worker 环境中安装 Codex CLI 并端到端验证

### TASK-20260701-001：Agent 测试资产契约与完整执行闭环修复

- 状态：已完成
- 完成日期：2026-07-02
- 负责人：AI
- 目标：统一各模块字段、配置 Schema、运行快照和下游消费，使各模块可独立使用并组成真实 Agent 测试闭环。
- 审查证据：`docs/audits/2026-07-01-module-configuration-flow/`
- 设计规格：`docs/superpowers/specs/2026-07-01-agent-test-asset-contract-and-execution-loop-design.md`
- 实施计划：`docs/superpowers/plans/2026-07-01-agent-test-asset-contract-and-execution-loop.md`
- 实现摘要：
  - Task 1-7、13 全部完成
  - Task 7 Step 4（四步计划编辑器 UI）：重构 TestPlanVersionDialog 为四步向导（资产→执行→评估→门禁），含步骤指示器和就绪检查徽标
  - Task 13 Step 5（完整提交）：全量门禁通过，文档更新
- 变更文件：
  - `apps/web/src/features/test-plans/test-plan-version-dialog.tsx`（重写为四步向导）
  - `apps/web/src/features/test-plans/test-plan-detail.tsx`（传递 planId/projectId）
  - `apps/web/src/features/test-plans/tests/test-plan-list.test.tsx`（适配四步交互）
- 验证记录：
  - `make verify`：format/lint ✅、typecheck ✅、test (201 Web + 581 Python = 782 passed) ✅、build ✅、architecture ✅
- 遗留问题：
  - PostgreSQL 约束测试需要可用测试数据库
  - 外部真实被测 Agent 端到端需要目标 Agent
- 下一步：PostgreSQL 迁移测试、Worker 协议回放测试、浏览器 E2E

### TASK-20260701-008：Agent 框架增强——流式超时/取消信号传播

- 状态：已完成
- 完成日期：2026-07-02
- 负责人：AI
- 目标：Agent 框架层面增强 Temporal Workflow 取消信号传播、超时对齐、callback token 轮换
- 实现摘要：
  - TemporalInvoker.stream() 改为 start_workflow() 获取 WorkflowHandle，支持外部取消
  - 新增 StreamContext 类型 + ModelInvoker.cancel_workflow() 协议方法
  - Worker ModelStreamingWorkflow 添加取消处理、ActivityCancellationType 优雅取消
  - Worker stream_model Activity 捕获 CancelledError、返回已累积内容不抛异常
  - Callback token 每次请求随机生成（per-request rotation）
  - queue.get() 超时从 timeout+30 对齐到 timeout+15
- 变更文件：
  - `apps/control-api/src/agenttest/modules/model_configs/application/ports.py`（新增 StreamContext、cancel_workflow）
  - `apps/control-api/src/agenttest/modules/model_configs/infrastructure/temporal_invoker.py`（start_workflow + cancel_workflow）
  - `apps/control-api/src/agenttest/modules/model_configs/public.py`（导出 StreamContext）
  - `apps/control-api/src/agenttest/modules/test_agent/application/temporal_model.py`（接入取消信号传播）
  - `workers/model-runner/src/agenttest_model_runner/workflow.py`（取消处理 + ActivityCancellationType）
  - `workers/model-runner/src/agenttest_model_runner/activities.py`（asyncio.CancelledError 优雅处理）
- 验证记录：
  - ruff ✅、pytest unit/test_agent (27 passed) ✅、contract (100 passed) ✅
- 遗留问题：
  - Callback token 热切换需要 Worker 侧支持（当前保留 per-request token，足够安全）
- 下一步：无需后续



### TASK-20260630-003：超级测试 Agent 与全平台编排

- 状态：待验证
- 开始日期：2026-06-30
- 负责人：AI
- 分支：`codex/feature/super-test-agent`
- 关联需求：PRD 1、5、6、8、10、11、12、13、14、15、19
- 目标：将 Test Agent 升级为可委派专业子 Agent、操作全平台真实资产的项目级超级 Agent，形成对话、控制台、执行、评测与安全红队闭环。
- 设计规格：`docs/superpowers/specs/2026-06-30-super-test-agent-platform-orchestration-design.md`
- 实施计划：`docs/superpowers/plans/2026-06-30-super-test-agent-platform-orchestration.md`
- 实现摘要：
  - 新增 `0012` 编排迁移：会话、任务、事件、确认、资产关联及被测 Agent 会话/轮次全部项目隔离持久化。
  - 普通问候和多轮对话由项目默认真实模型生成；Model Runner 解析真实 SSE 并逐块回调，Control API 按序持久化，前端增量渲染并支持断线补发。
  - 注册 26 项版本化能力，覆盖智能体、环境/凭证、数据集/用例、计划、执行、评分器、实验、安全、人工审核和发布门禁；写操作要求确认并记录真实资产 ID。
  - 新增独立被测 Agent 对话工作区，通过 API Runner 和通用 HTTP Adapter 支持 sync/stream/poll，持久化多轮输出、脱敏 Trace、耗时、Token/评分字段和错误。
  - 被测 Agent 对话轮次可转换为专业控制台中的真实回归用例，不生成聊天副本或占位资产。
  - 修复模型配置重名 409、旧迁移离线生成、项目/成员 PostgreSQL 插入顺序、跨模块边界、Web 历史测试/Lint 和后端类型基线。
- 验证记录：
  - `make verify`：通过（format、lint、mypy/TypeScript、全量测试、build、architecture、OpenAPI drift）。
  - Python：447 passed、4 skipped；Web：201 passed；生成客户端：1 passed。
  - 隔离 PostgreSQL：12 passed，包含空库升级、降级重建、约束、审计和项目隔离；迁移 head 为 `0012`。
  - 真实性扫描、`git diff --check`、定向密钥扫描：通过；命中项仅为测试假密钥。
- 待验证：
  - 使用用户新提供且已轮换的供应商 Key 与真实被测 Agent 做最终端到端验收；不使用聊天中泄露的旧 Key。
- 验收方式：真实模型流式对话、会话恢复、子 Agent 编排、全资产链、权限/幂等/审计、Temporal 重放与真实被测 Agent 端到端验收。

### TASK-20260630-002：全接口认证与运行依赖一致性修复

- 状态：待验证
- 开始日期：2026-06-30
- 负责人：AI
- 分支：`codex/fix/local-model-config`
- 关联需求：PRD 5、8.3、8.6、15.2、15.3、19.3、19.6
- 目标：统一所有浏览器 API 的本地 Cookie、认证异常与运行依赖失败语义，防止基础设施问题伪装成 401 或成功 Run。
- 计划修改：共享认证守卫、Run 编排端口/适配器/API、Web 问题详情展示、生产真实性门禁和运行文档。
- 验收方式：认证与 Run 单元/契约测试，模型配置契约测试，Web 错误展示测试，真实性扫描、类型检查和构建。
- 实施计划：`docs/superpowers/plans/2026-06-30-api-runtime-readiness-audit.md`
- 实现摘要：
  - `Settings` 在 local 环境允许 HTTP Cookie，非 local 环境仍强制 Secure；本地初始化脚本生成并幂等复用权限为 `0600` 的模型凭证主密钥。
  - 共享认证守卫只把 `InvalidSessionError` 映射为 401，数据库或身份服务异常继续进入 500 监控链路。
  - Trace Diff 只把真实 `ProjectNotFoundError` 映射为 404，不再吞掉任意异常。
  - Run 编排新增运行时 readiness；缺少 Temporal 时创建/取消均返回 RFC 7807 503，且创建前不写入 Run。
  - Model Runner 未配置或不可达时，模型连接与裁判接口返回稳定且可操作的安全错误详情。
  - Web 新增统一 `ApiProblemError`、`problemMessage`、`responseProblem`，覆盖模型、Run、安全扫描、审核、评分器、实验、门禁、测试计划、产物和 Test Agent 手写 API。
  - 生产真实性门禁新增：禁止广义认证/404 异常吞噬、假本地 Workflow 和 `Failed ...` 泛化前端错误。
  - 补齐 API Runner 可执行启动入口，与控制面共享 Temporal namespace/任务队列默认值，注册真实 Run Workflow 及 HTTP、回传、Browser Harness、Playwright Activities。
- 验证记录：
  - 定向后端：35 passed；定向前端：13 passed；生产真实性门禁：2 passed、脚本退出码 0。
  - 后端全量：388 passed、4 skipped；仅 1 个既有模块边界测试失败（5 条旧 identity 跨模块导入）。
  - Web 全量：192 passed、7 个既有失败；TypeScript 与生产构建通过。
  - API Runner：29 passed；新入口 Ruff 与 mypy 通过；实际连接本机 Temporal 后成功进入 Worker 消费循环。
  - Ruff 定向检查、定向 ESLint、`git diff --check`：通过。
  - 全量 Web Lint 保持既有 3 errors/11 warnings；全量 API Runner mypy 保持 Playwright Activity 1 个既有类型错误；全量后端 mypy 保持实验统计模块 7 个既有错误。
- 未验证与风险：
  - Temporal 已可连接，API Runner 启动已验证但未作为长驻进程保持；Model Runner 未启动，也未使用撤销后重新生成的供应商 Key，因此端到端调用继续列为待验证。
- 下一步：
  - 启动完整本地执行栈，用户在页面录入新供应商 Key 后依次验证“测试连接 → 默认模型 → Agent 对话 → Run 执行”。

### TASK-20260630-001：生产路径 Mock、占位与硬编码清理

- 状态：待验证
- 开始日期：2026-06-30
- 负责人：AI
- 分支：`codex/fix/production-truthfulness`
- 后续修复分支：`codex/fix/local-model-config`
- 关联需求：PRD 5、8.3、8.6、8.7、8.9、15.2、15.3、15.4、19.3、19.4、19.6
- 关联架构：技术架构规范 2、4、5、7、8、9、14、15、21
- 目标：移除生产路径中的自动 Mock、演示数据、假成功、临时事实源和运行时硬编码，使已保留能力真实执行或明确失败。
- 计划修改：执行 Worker fail-closed、安全扫描、报告与失败用例生成、测试 Agent 会话持久化、伪 Playwright Agent 入口、Web API 配置、Compose 镜像与审计门禁。
- 验收方式：生产路径审计测试，后端/Worker 单元与契约测试，项目隔离和迁移验证，Web 全量质量门禁、OpenAPI 漂移和 Compose 配置校验。
- 设计规格：`docs/superpowers/specs/2026-06-30-production-truthfulness-cleanup-design.md`
- 实施计划：`docs/superpowers/plans/2026-06-30-production-truthfulness-cleanup.md`
- 实现摘要：
  - 新增生产真实性扫描门禁，禁止自动 Mock、演示响应、内存事实源、假成功、浮动运行时版本和生产 localhost 回退。
  - Playwright 与 Browser Harness 缺少依赖时 fail-closed；保留用户主动配置的网络 Mock/故障注入能力。
  - Promptfoo 安全扫描必须提交真实 Agent 地址，输出解析失败或工具不可用时明确失败，并默认阻止本机/私网目标。
  - 报告导出和“失败转回归”改为读取项目隔离的真实 Run、RunCase 与 TestCase 数据。
  - 测试 Agent 会话和消息落库，新增 `0010_test_agent_sessions`；确认计划不再伪装为已启动运行。
  - 删除未接真实运行时的 Playwright Agent、未来扫描器、推荐器和 Canvas 独立模型密钥路径；模型判断统一走项目级 Model Runner。
  - Web API 地址收口到单一配置源，生产默认同源；上游失败不再返回空数组；CI 报告接口改为项目级鉴权接口。
  - 固定 Compose 镜像版本，收紧 CORS、Session Cookie 和非本地内部 Token 默认值，更新 OpenAPI、生成客户端、PRD、架构和 README。
  - 后续修复 localhost 模型配置失败：本地 Cookie 默认允许 HTTP，生产仍强制 Secure；新增幂等本地环境初始化脚本，生成并复用 Git 忽略的模型凭证主密钥。
- 主要变更：
  - `apps/control-api/src/agenttest/modules/{security,reports,datasets,test_agent,runs}/**`
  - `apps/control-api/migrations/versions/0010_test_agent_sessions.py`
  - `workers/api-runner/**`
  - `apps/web/src/lib/api/**`、相关 Feature API 与安全扫描/测试 Agent 页面
  - `scripts/check_production_truthfulness.py`、`infra/compose/compose.yaml`、`.github/workflows/templates/test-report.yml`
  - OpenAPI、生成客户端、PRD、架构、README 与 CI 接入文档
- 验证记录：
  - 生产真实性扫描：1 passed，脚本退出码 0
  - 后端全量：381 passed、4 skipped；1 个既有模块边界测试失败（5 条旧跨模块 identity 导入）
  - 变更域后端：35 passed；API Runner：26 passed
  - Web 变更域：32 passed；全量 187 passed、7 个既有失败
  - Web TypeScript、生成客户端 TypeScript、Web 生产构建：通过
  - 变更文件 Ruff、定向 ESLint、`uv lock --check`、Compose 配置渲染、`git diff --check`：通过
  - 全量 Web Lint：4 个既有错误、11 个既有警告；全量 mypy：实验统计模块 7 个既有错误
  - 本地配置回归：Settings、本地环境初始化、认证与模型配置契约共 17 passed；当前配置确认使用 AES-GCM Cipher
- 未验证与风险：
  - 4 项 PostgreSQL 集成测试因缺少隔离数据库跳过；迁移仅验证离线 PostgreSQL SQL。
  - 未连接真实 Promptfoo、Temporal、Playwright 和 Browser Harness，外部协议兼容性仍需部署环境验收。
  - 仓库既有架构边界、Web 测试/Lint 与 mypy 基线问题不属于本次真实性清理，未顺手扩大修改范围。
- 下一步：
  - 在完整基础设施环境运行 PostgreSQL 迁移/隔离、Promptfoo 扫描及浏览器执行 E2E；通过后将任务标记为已完成。

---

## 4. 已完成任务

### TASK-20260629-005：统一账户与帮助体验优化

- 状态：已完成
- 开始日期：2026-06-29
- 完成日期：2026-06-29
- 负责人：AI
- 分支：`codex/feature/account-experience-polish`
- 关联需求：PRD 4.2、5、6.1、7、12.2、15
- 关联架构：前端 Feature 边界、生成 API Client、安全与测试规范
- 目标：采用 A 方案建立统一账户中心，并完整优化帮助、反馈及顶部账户相关入口的 UI 与交互。
- 设计规格：`docs/superpowers/specs/2026-06-29-account-help-experience-polish-design.md`
- 实施计划：`docs/superpowers/plans/2026-06-29-account-help-experience-polish-plan.md`
- 实现摘要：
  - Task 1: 创建后端源码编译回归测试
  - Task 2: 验证现有 contract 测试通过
  - Task 3: 更新 OpenAPI 并生成 API 客户端
  - Task 4: 创建账户中心页面组件（响应式两栏布局）
  - Task 5: 创建账户中心各 section 组件（profile、preferences、notifications、security）
  - Task 6: 集成通知下拉菜单和用户下拉菜单更新
  - Task 7: 创建帮助搜索和反馈表单组件
  - Task 8: 更新文档
  - 后续修复: 更新 TestPlanConfigRequest schema，添加 baseline_run_id、max_retries、release_gate 字段
  - 后续修复: 添加前端单元测试覆盖（4 个测试文件，19 个测试用例）
- 变更文件：
  - `apps/control-api/src/agenttest/modules/test_plans/api/schemas.py`
  - `apps/web/src/lib/utils.ts`
  - `apps/web/src/features/account/*`（新增）
  - `apps/web/src/features/help/*`（新增）
  - `apps/web/src/components/layout/notification-dropdown.tsx`（新增）
  - `apps/web/src/components/layout/user-dropdown.tsx`
  - `apps/web/src/components/layout/app-shell.tsx`
  - `apps/web/src/app/(platform)/account/page.tsx`（新增）
  - `apps/web/src/app/(platform)/profile/page.tsx`
  - `apps/web/src/app/(platform)/settings/page.tsx`
  - `apps/web/src/app/(help)/docs/page.tsx`
  - `apps/web/src/app/(help)/feedback/page.tsx`
  - `apps/web/src/features/account/tests/account-center.test.tsx`（新增）
  - `apps/web/src/features/help/tests/help-search.test.tsx`（新增）
  - `apps/web/src/features/help/tests/feedback-form.test.tsx`（新增）
  - `apps/web/src/components/layout/tests/notification-dropdown.test.tsx`（新增）
  - `docs/api/openapi.json`
  - `packages/generated-api-client/src/client/*`
- 验证记录：
  - 后端源码编译测试：通过
  - 前端类型检查：通过
  - 前端单元测试：新添加的 19 个测试全部通过（总体 167 passed, 9 failed 为已存在的测试问题）
- 技术债：
  - 已存在的测试失败问题需修复（project-overview.test.tsx 等）
  - 桌面和窄屏优化前后视觉对比
- 当前进度：Task 1-7 已完成，账户中心、帮助搜索、反馈表单已实现，继续 Task 8
- 已知问题：test-plan-version-dialog.tsx 存在 baseline_run_id 类型错误

---

### TASK-20260629-003：阶段三 — 人工审核深度集成

- 状态：已完成
- 开始日期：2026-06-29
- 完成日期：2026-06-29
- 负责人：AI
- 关联需求：PRD 8.11
- 关联架构：技术架构规范 DDD 模块架构
- 目标：实现低置信度自动收集、A/B 偏好选择、Rubric 多维评分
- 计划修改：
  - `apps/control-api/src/agenttest/modules/reviews/domain/auto_collector.py`（新增）
  - `apps/control-api/tests/unit/reviews/test_auto_collector.py`（新增）
  - `apps/web/src/features/reviews/ab-preference.tsx`（新增）
  - `apps/web/src/features/reviews/rubric-editor.tsx`（新增）
  - `apps/web/src/features/reviews/tests/ab-preference.test.tsx`（新增）
  - `apps/web/src/features/reviews/tests/rubric-editor.test.tsx`（新增）
- 验收方式：
  - 后端测试通过（5+）
  - 前端测试通过
  - 类型检查通过
  - Lint 检查通过
  - 构建验证通过

#### 子任务进度

- [x] Task 3.1: 低置信度自动收集逻辑（0.5 天）
- [x] Task 3.2: A/B 偏好选择 UI（1 天）
- [x] Task 3.3: Rubric 多维评分配置（0.5 天）

#### 阶段三验证结果

- 后端测试：9 个通过
- 前端测试：20 个通过
- 类型检查：通过
- 构建：通过

---

### TASK-20260629-004：阶段四 — 测试计划高级配置

- 状态：已完成
- 开始日期：2026-06-29
- 完成日期：2026-06-29
- 负责人：AI
- 关联需求：PRD 8.5
- 关联架构：技术架构规范 DDD 模块架构
- 目标：实现评分器权重配置、阈值校验、成本预算控制
- 计划修改：
  - `apps/control-api/src/agenttest/modules/test_plans/domain/threshold_validator.py`（新增）
  - `apps/control-api/src/agenttest/modules/test_plans/domain/budget_controller.py`（新增）
  - `apps/control-api/tests/unit/test_plans/test_threshold_validator.py`（新增）
  - `apps/control-api/tests/unit/test_plans/test_budget_controller.py`（新增）
  - `apps/web/src/features/test-plans/scorer-weight-config.tsx`（新增）
  - `apps/web/src/features/test-plans/budget-display.tsx`（新增）
  - `apps/web/src/features/test-plans/tests/scorer-weight-config.test.tsx`（新增）
  - `apps/web/src/features/test-plans/tests/budget-display.test.tsx`（新增）
- 验收方式：
  - 后端测试通过（15+）
  - 前端测试通过（15+）
  - 类型检查通过
  - 构建验证通过

#### 子任务进度

- [x] Task 4.1: 评分器权重配置 UI（0.5 天）
- [x] Task 4.2: 阈值校验逻辑（0.5 天）
- [x] Task 4.3: 成本预算控制（0.5 天）

#### 阶段四验证结果

- 后端测试：17 个通过（阈值校验 10 个 + 预算控制 7 个）
- 前端测试：15 个通过（权重配置 7 个 + 预算展示 8 个）
- 类型检查：通过
- 构建：通过

---

#### 阶段二完成摘要

### TASK-20260629-002：阶段二 — 实验对比统计增强

- 状态：已完成
- 开始日期：2026-06-29
- 负责人：AI
- 关联需求：PRD 8.10
- 关联架构：技术架构规范 DDD 模块架构
- 目标：实现统计分析 API 和对比 UI，支持 P50/P95、退化项高亮、聚合视图
- 计划修改：
  - `apps/control-api/src/agenttest/modules/experiments/domain/statistics.py`（新增）
  - `apps/control-api/src/agenttest/modules/experiments/api/router.py`（修改）
  - `apps/control-api/tests/unit/experiments/test_statistics.py`（新增）
  - `apps/web/src/features/experiments/experiment-compare.tsx`（重构）
  - `apps/web/src/features/experiments/degradation-highlight.tsx`（新增）
  - `apps/web/src/features/experiments/aggregation-view.tsx`（新增）
  - 相关测试文件
- 验收方式：
  - 后端测试通过（5+）
  - 前端测试通过
  - 类型检查通过
  - Lint 检查通过
  - 构建验证通过

#### 子任务进度

- [x] Task 2.1: 统计分析后端 API（0.5 天）
- [x] Task 2.2: 对比 UI 增强（1 天）

#### 阶段一完成摘要

- 分支：`codex/feature/phase1-result-workbench`
- 子任务完成情况：
  - [x] Task 1.1: Trace 树形可视化组件（1 天）
  - [x] Task 1.2: 结果工作台三栏布局（0.5 天）
  - [x] Task 1.3: 产物预览集成（0.5 天）
- 验证结果：
  - 测试：88 个通过（18 个测试文件）
  - 类型检查：通过
  - Lint：0 错误，1 警告
  - 构建：通过

## 3A. 最近完成的任务

### TASK-20260629-002：阶段二 — 实验对比统计增强

- 状态：已完成
- 完成日期：2026-06-29
- 分支：`codex/feature/phase2-experiment-stats`
- 关联需求：PRD 8.10
- 关联架构：技术架构规范 DDD 模块架构
- 实现摘要：
  - 创建 `statistics.py` 统计分析服务，支持 P50/P95 计算、退化项识别
  - 创建 `MetricStatistics` 和 `ExperimentStatistics` 值对象
  - 创建 `calculate_statistics` 和 `identify_degradation` 函数
  - 添加 `/statistics` API 端点，支持单运行统计和实验对比统计
  - 创建 `DegradationHighlight` 退化项高亮组件（红色背景、变化百分比）
  - 创建 `AggregationView` 聚合视图组件（状态卡片、延迟/分数/成本 P50/P95）
  - 创建 `ExperimentCompare` 实验对比页面组件
  - 编写 15 个后端测试 + 11 个前端测试
- 变更文件：
  - `apps/control-api/src/agenttest/modules/experiments/domain/statistics.py`（新增）
  - `apps/control-api/src/agenttest/modules/experiments/api/router.py`（修改）
  - `apps/control-api/tests/unit/experiments/test_statistics.py`（新增）
  - `apps/web/src/features/experiments/degradation-highlight.tsx`（新增）
  - `apps/web/src/features/experiments/aggregation-view.tsx`（新增）
  - `apps/web/src/features/experiments/experiment-compare.tsx`（新增）
  - `apps/web/src/features/experiments/tests/experiment-compare.test.tsx`（新增）
- 数据库/API/配置变化：
  - 新增 API：`GET /api/v1/projects/{project_id}/experiments/statistics`
- 验证记录：
  - `uv run pytest apps/control-api/tests/unit/experiments/`：23 个测试通过
  - `pnpm --filter @warmy/web test`：99 个测试通过（19 个测试文件）
  - `pnpm --filter @warmy/web typecheck`：通过
  - `pnpm --filter @warmy/web lint`：0 错误，1 警告
  - `pnpm --filter @warmy/web build`：通过
- 遗留问题：无
- 下一步：可进入阶段三（人工审核深度集成）

### TASK-20260629-001C：阶段一 — 产物预览集成

- 状态：已完成
- 完成日期：2026-06-29
- 分支：`codex/feature/phase1-result-workbench`
- 关联需求：PRD 6.3、6.6、8.5
- 关联架构：技术架构规范前端模块架构、组件设计规范
- 实现摘要：
  - 创建 `ArtifactPreview` 产物预览主组件，支持图片/视频/文件分类筛选、文件大小显示、下载功能
  - 创建 `ImageViewer` 图片查看器组件，支持缩放（+/重置）、全屏模式
  - 创建 `VideoPlayer` 视频播放器组件，支持播放/暂停、音量控制、进度显示、下载
  - 编写 28 个测试用例覆盖所有组件功能
- 变更文件：
  - `apps/web/src/features/runs/artifact-preview.tsx`（新增）
  - `apps/web/src/features/runs/image-viewer.tsx`（新增）
  - `apps/web/src/features/runs/video-player.tsx`（新增）
  - `apps/web/src/features/runs/tests/artifact-preview.test.tsx`（新增）
  - `apps/web/src/features/runs/tests/image-viewer.test.tsx`（新增）
  - `apps/web/src/features/runs/tests/video-player.test.tsx`（新增）
- 数据库/API/配置变化：无
- 验证记录：
  - `pnpm --filter @warmy/web test`：88 个测试通过（18 个测试文件）
  - `pnpm --filter @warmy/web typecheck`：通过
  - `pnpm --filter @warmy/web lint`：0 错误，1 警告（img 元素警告，预期行为）
  - `pnpm --filter @warmy/web build`：通过
- 遗留问题：无
- 下一步：阶段一验证完成，可进入阶段二

### TASK-20260629-001B：阶段一 — 结果工作台三栏布局

- 状态：已完成
- 完成日期：2026-06-29
- 分支：`codex/feature/phase1-result-workbench`
- 关联需求：PRD 6.3、6.6、8.5
- 关联架构：技术架构规范前端模块架构、组件设计规范
- 实现摘要：
  - 创建三栏布局结果工作台组件 `RunResultWorkbench`
  - 实现左栏：用例列表、状态筛选（全部/通过/失败/错误）、搜索、用例卡片
  - 实现中栏：用例详情、Trace 树形可视化集成、输入输出展示
  - 实现右栏：评分结果展示、状态统计、快速操作按钮
  - 支持用例选择、状态切换、响应式布局
- 变更文件：
  - `apps/web/src/features/runs/run-result-workbench.tsx`（新增）
  - `apps/web/src/features/runs/tests/run-result-workbench.test.tsx`（新增）
- 数据库/API/配置变化：无
- 验证记录：
  - `pnpm --filter @warmy/web test`：60个测试通过（15个测试文件）
  - 包括8个新增的三栏布局测试用例
- 遗留问题：无
- 下一步：Task 1.3 产物预览集成

### TASK-20260628-012：集中修复 Web 测试 TypeScript 错误

- 状态：已完成
- 完成日期：2026-06-28
- 分支：`codex/fix/web-test-errors`
- 关联需求：PRD 第 12.3 章前端质量、端到端测试
- 关联架构：技术架构第 6.6、15.3、17.1、21.1 章
- 实现摘要：
  - 清除 `test-case-editor.tsx` 误拼接的旧版重复组件，消除 28 个重复声明错误。
  - `priority`、`risk_level`、`test_group` 直接使用生成 API Client 的联合类型，消除 3 个不安全字符串赋值错误。
  - 补齐 Playwright 专用 TypeScript 配置中的 Node/Playwright 类型，并隔离 Vitest 与 Playwright E2E 测试目录。
  - 增加增强测试用例字段提交回归测试，修正运行详情分栏后的测试断言。
  - 清理全仓 Web Lint 发现的 5 个 Effect 同步状态错误和 20 个未使用代码警告；异步初始加载增加卸载保护。
- 变更文件：
  - `apps/web/src/features/datasets/test-case-editor.tsx`
  - `apps/web/src/features/datasets/tests/dataset-list.test.tsx`
  - `apps/web/playwright/tsconfig.json`
  - `apps/web/vitest.config.ts`
  - `apps/web/src/features/runs/tests/run-center.test.tsx`
  - Web experiments/gates/reviews/scorers/security 初始加载与无用代码清理相关文件
- 数据库/API/配置变化：
  - 数据库和 API 无变化。
  - Playwright TypeScript 增加 `node`、`@playwright/test` types；Vitest 排除 `tests/e2e/**`。
- 验证记录：
  - `pnpm --filter @warmy/web typecheck`：通过。
  - `pnpm --filter @warmy/web exec tsc --noEmit -p playwright/tsconfig.json`：通过。
  - `pnpm --filter @warmy/web lint`：通过，0 错误 0 警告。
  - `pnpm --filter @warmy/web test`：13 个测试文件、44 条测试通过。
  - `pnpm --filter @warmy/web exec playwright test --list`：成功收集 4 个文件、12 条测试。
  - `pnpm --filter @warmy/web build`：通过。
  - 本任务触及文件 Prettier 检查：通过。
- 遗留问题：
  - 全仓 `prettier --check .` 仍包含本任务范围外的历史格式债，未做 62 文件机械重排。
  - 真实 Playwright 浏览器旅程依赖后端、数据库与测试账号，本任务未执行。
- 下一步：
  - 由用户决定提交、合并或继续修复全仓历史格式债。

### TASK-20260627-003：阶段一 — Agent 与版本管理深化（PRD 8.2）

- 状态：已完成
- 完成日期：2026-06-28
- 分支：codex/feature/phase1-agent-versions
- 关联需求：PRD 第 8.2 章
- 目标：Agent 详情页完整化，支持版本生命周期管理
- 实现摘要：
  - 后端已包含所有 PRD 8.2 字段（AgentConfig：git_commit、model_params、tools、knowledge_version、timeout、max_steps、cost_limit）
  - 版本对比 API：`GET /{agent_id}/versions/{v1_id}/diff/{v2_id}`
  - 基线标记 API：`PATCH /{agent_id}/current-version` 和 `PATCH /{agent_id}/baseline-version`
  - 前端新增 version-detail-drawer.tsx（版本详情抽屉）
  - 前端新增 version-diff-view.tsx（版本对比视图）
  - 前端增强 agent-detail.tsx（版本列表显示当前/基线标记、集成详情抽屉和对比视图）
  - schemas.py 更新：AgentResponse 新增 current_version_id 和 baseline_version_id 字段
  - API 客户端类型更新
- 验证记录：
  - 后端测试：295 passed / 3 failed（已有问题）/ 4 skipped
- 变更文件：
  - `apps/control-api/src/agenttest/modules/agents/api/schemas.py`
  - `apps/web/src/features/agents/agent-detail.tsx`
  - `apps/web/src/features/agents/version-detail-drawer.tsx`（新增）
  - `apps/web/src/features/agents/version-diff-view.tsx`（新增）
  - `packages/generated-api-client/src/client/types.gen.ts`
  - `docs/当前任务.md`

## 3A. 最近完成的里程碑

### TASK-20260625-006：M2 测试资产管理

- 状态：已完成
- 完成日期：2026-06-25
- 负责人：AI
- 关联需求：PRD Agent、数据集、用例和测试计划相关章节
- 关联架构：技术架构规范模块边界、不可变版本、项目隔离
- 目标：建立 Agent、Dataset、TestCase 和 TestPlan 的领域模型、API 和前端管理
- 验收方式：单元/集成/契约测试、迁移验证、前端组件测试、Lint、类型检查、构建
- 实现摘要：
- 当前进展：
  - 已完成 M2 详细实施计划（813 行）。
  - 已完成数据库迁移 0002_test_assets（agents、datasets、test_plans、environments 建表）。
  - 已完成 Agents、Dataset、TestPlan、EnvironmentTemplate 四大领域模型和应用层。
  - 新分支基线 133 个测试通过，4 个 PostgreSQL 测试跳过。
  - 当前子任务：详细计划 Task 6 测试资产 API 路由。
  - Agents API 已完成：9 个 CRUD/版本发布端点、项目访问适配、CSRF 和应用工厂注册。
  - Agents 契约与架构测试 8 个通过，Ruff 和 mypy 通过。
  - Dataset API 已完成：14 个数据集/版本/用例/导入导出端点、项目路径隔离、不可变版本、CSRF 和应用工厂注册。
  - Dataset 契约测试 6 个通过；Agents + Dataset + 架构测试 13 个通过；146 个源文件 mypy 和本次改动 Ruff 检查通过。
  - TestPlan API 已完成：9 个计划 CRUD/版本发布端点、项目路径隔离、不可变版本、CSRF 和应用工厂注册。
  - Agents + Dataset + TestPlan + 架构测试 17 个通过；148 个源文件 mypy 和本次改动 Ruff 检查通过。
  - EnvironmentTemplate API 已完成：5 个模板 CRUD 端点、项目路径隔离、CSRF 和应用工厂注册。
  - Task 6 四类测试资产 API 路由全部完成；联合契约与架构测试 21 个通过。
  - 全量后端回归 150 个通过、4 个 PostgreSQL 测试跳过；150 个源文件 mypy 和本次改动 Ruff 检查通过。
  - 仓库全量 Ruff 仍有 21 个基线既有问题（迁移换行、旧安全/领域测试导入与 UTC 写法），未在本任务中混入修复。
  - Task 7 已完成：OpenAPI 已包含四类测试资产 API，TypeScript Client 已重新生成，并新增 `docs/api/test-assets.md`。
  - OpenAPI 与客户端重复生成哈希一致；生成客户端 Vitest 1 个通过，TypeScript 类型检查通过，Lockfile 无漂移。
  - Task 8 已完成：Agent 列表、创建、详情、版本创建/编辑、发布确认、发布锁定和真实 API 查询接入。
  - Agent 组件测试 4 个通过；Web 全量 28 个测试、ESLint、TypeScript 和 Next.js Build 通过。
  - 为恢复 Web 质量门禁，清除了历史重复拼接的用户组件/测试，隔离 Vitest 与 Playwright 测试，并修复 Playwright Locator 类型错误。
  - Task 9 已完成：Dataset 列表、版本标签、TestCase 新增/编辑/删除、JSON 编辑、JSON/JSONL/CSV 导入导出、发布确认和发布后只读。
  - Dataset 组件测试 6 个通过；Web 全量 34 个测试、ESLint、TypeScript 和 Next.js Build 通过。
  - Task 10 已完成：TestPlan 列表、版本运行参数、已发布 Agent/Dataset 版本选择、环境模板引用、发布确认和发布后锁定。
  - TestPlan 组件测试 4 个通过；Web 全量 38 个测试、ESLint、TypeScript 和 Next.js Build 通过。
  - Task 11 已完成：左侧项目导航接入 Agents、Datasets、TestPlans，项目概览展示三类真实资产数量。
  - Agent 流式对话页预留的 `workspaceMode="agent"` 三栏结构保持不变。
  - Web 全量 38 个测试、ESLint、TypeScript 和 Next.js Build 通过。
  - 当前子任务：详细计划 Task 12 验收测试与文档。
  - 待实现：测试资产 E2E、README、最终 M2 验收和环境依赖验证记录。
  - 2026-06-25 补充：M2 四大模块全部代码已添加中文注释（按架构规范第 23 章）。

---

## 4. 待开始任务

当前无其他待开始任务。

---

## 5. 阻塞任务

### TASK-20260629-006：帮助中心与个人资料体验二次优化

- 状态：待验证
- 已实现并合入 `main`；任务范围格式、Lint、类型检查、25 项针对性测试和生产构建通过。
- 阻塞原因：全仓仍有 7 项既有 Web 测试失败，且任务范围外存在既有 Lint 与格式问题。
- 恢复条件：修复或批准豁免既有 Web 质量问题后，重新运行全仓验证并关闭任务。

---

## 6. 待验证任务

### TASK-20260629-007：项目级大模型配置与真实 Agent 评测调用

- 状态：待验证
- 完成日期：2026-06-29（代码完成）
- 实现摘要：项目模型配置、AES-GCM 凭证保护、Model Runner、真实测试 Agent/文本裁判/视觉裁判调用、连接测试和 Web 配置页面已实现。
- 已验证：任务范围后端/Worker/前端测试、Ruff、Mypy、ESLint、类型检查、PostgreSQL 离线迁移 SQL、生产构建及目标 Mock/占位扫描通过。
- 未验证：真实 PostgreSQL + Temporal + Model Runner + 供应商凭证拓扑、登录后关键 E2E；全仓仍有既有后端 2 项与 Web 7 项失败。
- 恢复条件：具备真实拓扑和登录态后补验，并在全仓门禁收敛后关闭。

### TASK-20260626-001：M3 通用 HTTP Agent 测试闭环

- 状态：待验证（代码完成）
- 完成日期：2026-06-27（代码完成）
- 关联需求：PRD 第 7.3、8.6、8.8、8.9、15、17.1、19.3 章
- 目标：Run/RunCase 创建/执行/进度/取消/结果/Trace + Artifact 产物
- 实现摘要：
  - Run/RunCase 领域模型、迁移、幂等创建/取消 API
  - workers/api-runner 包、HTTP Agent Adapter、Temporal Workflow
  - 控制面结果回写 + X-Internal-Token 保护
  - 前端运行中心（统计卡/搜索筛选/SSE 退避重连）
  - 前端运行详情（执行摘要/进度条/用例统计）
  - Artifact 产物全链路（API + 前端上传/列表/下载）
- 验证记录：
  - 后端 111 tests ✅ / ruff ✅ / mypy ✅
  - 前端 TypeCheck ✅ / Lint ✅ / Build ✅
- 未验证：
  - 真实 PostgreSQL 迁移/约束
  - Docker Compose + Temporal Server
  - Workflow replay/cancel/timeout

### TASK-20260625-003：实现登录、用户与项目权限

- 状态：待验证
- 完成日期：2026-06-25（代码完成）
- 关联需求：PRD 第 8.1、16、19.1 章
- 关联架构：技术架构规范第 7.7、7.8、8.3、8.4 章
- 实现摘要：
  - M1 Plan Task 1–15 全部代码完成并合并至 `main`（最终合并 `7ed042b`）。
  - Monorepo 工程基线、FastAPI 控制面、Next.js 平台壳、Compose 基础设施。
  - 身份领域、Alembic 迁移、Argon2id 登录、服务端 Session、CSRF 防护。
  - 超级管理员 CLI、用户 CRUD API、最后管理员保护、密码重置和软删除。
  - 项目生命周期、成员管理、跨项目 404、移除即时失权。
  - 审计日志、敏感字段脱敏、Unit of Work 事务一致性。
  - OpenAPI 稳定导出、TypeScript Client 生成和漂移检查。
  - ADR-0001 至 ADR-0008、GitHub Actions CI 和 Dependabot。
  - 前端登录、平台壳、项目概览、用户管理（含编辑/删除）。
  - Playwright E2E 测试、Session 安全测试、API 文档和 README。
- 变更文件：
  - `apps/control-api/`（全后端实现）
  - `apps/admin-cli/`（超级管理员 CLI）
  - `apps/web/`（前端实现）
  - `packages/generated-api-client/`（API Client）
  - `infra/compose/`（基础设施）
  - `.github/workflows/`（CI）
  - `docs/`（ADR、API 文档、Runbook）
  - `README.md`、`Makefile`、根工程配置
- 数据库/API/配置变化：
  - 首个 Alembic 迁移：users、user_credentials、user_sessions、projects、project_members、audit.audit_logs
  - API：auth、system/users、projects、audit 端点
  - CI：python-quality、web-quality、contract、compose、security 工作流
- 验证记录：
  - 后端 66 个单元/契约测试通过，4 个 PostgreSQL 测试跳过。
  - Ruff、mypy、架构边界检查通过。
  - 前端 19 个组件测试通过，Lint、TypeCheck、Build 通过。
  - OpenAPI 漂移检查通过。
  - Session 安全测试已编写（Token 哈希、CSRF、登录不泄露、Session 撤销/过期）。
  - Playwright E2E 测试已编写（登录、用户管理、项目隔离）。
  - **未验证**：PostgreSQL 空库迁移、降级再升级、真实约束、项目隔离和审计回滚（当前环境无 Docker）。
  - **未验证**：Docker Compose 启动和健康检查（当前环境无 Docker）。
  - **未验证**：GitHub Actions 工作流实际运行（尚未推送触发）。
  - **未验证**：Playwright E2E 和前端全量验证（当前环境无 Node.js）。
- 遗留问题：
  - TD-001 至 TD-006 见已知问题与技术债。
- 下一步：
  - 在有 Docker 的环境中补验全部环境依赖测试。
  - 验证通过后标记为已完成。

---

## 7. 已完成任务

### TASK-20260701-003：PydanticAI Phase 2 — Agent 实例 + Tool Calling 接入

- 状态：已完成
- 开始日期：2026-07-01
- 完成日期：2026-07-01
- 负责人：AI
- 关联需求：PRD 8.3、8.6、12、14、15
- 目标：创建自定义 PydanticAI Model（Temporal 适配器），用 `Agent` + `@agent.tool` 替代手动路由/规划，实现真正的 agent delegation + tool calling。
- 实施计划：`docs/superpowers/plans/2026-07-01-pydanticai-subagent-refactor.md`
- 实现摘要：
  - 扩展消息协议支持 tool calling：`InvocationMessage`/`ChatMessage` 新增 `tool_calls`、`tool_call_id`、`name` 可选字段，`content` 改为可选
  - Worker adapter + Temporal invoker 新增 `_serialize_messages()` 透传 tool 字段
  - 创建 `temporal_model.py`（276 行）：自定义 PydanticAI `Model`，实现 `request()`/`request_stream()`，双向消息格式转换（PydanticAI ↔ OpenAI-Compatible）
  - 重写 `super_agent.py`：`create_super_agent()` 工厂函数，9 个 delegation tool（每个委托给对应 SubAgent），保留 `SubAgentRouter` 回退
  - 重写 `conversation.py`：`_pydantic_plan_actions()` 使用 `agent.run()` 替代手动 `SubAgentRouter.route()`，`_plan_actions()` 保留为回退路径
  - 更新 `confirmation.py`：标注 PydanticAI 兼容性和 Phase 3+ 集成路径
  - `bootstrap/app.py` 无需修改（`SuperAgentConversation.__init__` 签名不变）
- 变更文件：
  - `apps/control-api/src/agenttest/modules/test_agent/application/temporal_model.py`（新增，276 行）
  - `apps/control-api/src/agenttest/modules/test_agent/application/super_agent.py`（重写，173→357 行）
  - `apps/control-api/src/agenttest/modules/test_agent/application/conversation.py`（重写，240→350 行）
  - `apps/control-api/src/agenttest/modules/test_agent/application/confirmation.py`（更新 docstring）
  - `apps/control-api/src/agenttest/modules/model_configs/application/ports.py`（扩展 InvocationMessage）
  - `apps/control-api/src/agenttest/modules/model_configs/infrastructure/temporal_invoker.py`（新增 _serialize_messages）
  - `workers/model-runner/src/agenttest_model_runner/contracts.py`（扩展 ChatMessage）
  - `workers/model-runner/src/agenttest_model_runner/adapter.py`（新增 _serialize_messages）
  - `workers/model-runner/src/agenttest_model_runner/activities.py`（透传 tool 字段）
- 数据库/API/配置变化：无（API 契约向后兼容，新字段均为 optional）
- 验证记录：
  - `ruff check`：通过（0 errors）
  - `pytest apps/control-api/tests/ --ignore=tests/architecture`：124 passed、5 skipped
  - `pytest apps/control-api/tests/unit/test_agent/`：27 passed
  - `pytest apps/control-api/tests/contract/`：3 passed
  - 1 个既有失败（migrations 迁移版本号）与本次无关
  - 3 个既有架构违规（adapters/platform.py）与本次无关
- 遗留问题：
  - PydanticAI 流式响应（`request_stream()`）当前为 stub 回退到非流式，待后续实现真正的 Temporal 流式管道
  - SubAgent delegation tool 暂不执行平台能力（仅返回 ActionPlan），待 Phase 3 升级为直接执行
- 下一步：
  - Phase 3：SubAgent tool 直接调用 `PlatformGateway` 执行平台能力（替代当前 ActionPlan + Orchestrator 模式）
  - 实现 `request_stream()` 真实流式 SSE → PydanticAI delta 事件映射

---

### TASK-20260701-004：PydanticAI Phase 3 — 流式响应 + SubAgent 工具执行

- 状态：已完成
- 开始日期：2026-07-01
- 完成日期：2026-07-01
- 负责人：AI
- 关联需求：PRD 8.3、8.6、12、14、15
- 目标：(A) 实现 TemporalModel 真实流式 `request_stream()`；(B) 为 SubAgent 添加 `@agent.tool` 函数，让 READ 能力直接通过 `PlatformGateway` 执行。
- 实施计划：Phase3-流式与工具执行
- 实现摘要：
  - Task 1：升级 `request_stream()` — 从 stub 改为句子级增量流式（按中文标点分块），新增 `_split_sentences()` 和 `_tool_call_delta_event()` 辅助函数
  - Task 2：为 9 个 SubAgent 添加 READ 领域工具 — `_make_read_tool()` 工厂函数动态生成 `@agent.tool` 函数（调用 `PlatformGateway.execute()`），13 个 READ 能力分布在 target_agent/environment/test_data/test_plan/execution/evaluation/experiment/security/review_gate 各域
  - Task 3：注入 `platform_gateway` — `SuperAgentConversation` 新增 `platform_gateway` 参数并透传到 `OrchestrationContext`，`bootstrap/app.py` 传入 `HandlerPlatformGateway` 实例
  - Task 4：更新 `_extract_action_intents_from_result()` — 文档标注 Phase 3 混合结果处理逻辑（`_ActionPlan` → 提取 / dict → 跳过）
  - Task 5：更新 `confirmation.py` — 新增 `tool_prepare_read_only()` PydanticAI Tool prepare 回调
- 变更文件：
  - `apps/control-api/src/agenttest/modules/test_agent/application/temporal_model.py`（升级 request_stream，+80 行）
  - `apps/control-api/src/agenttest/modules/test_agent/application/super_agent.py`（新增 _build_domain_read_tools / _make_read_tool / _tool_descriptions，+80 行）
  - `apps/control-api/src/agenttest/modules/test_agent/application/conversation.py`（新增 platform_gateway 参数 + 注入）
  - `apps/control-api/src/agenttest/modules/test_agent/application/confirmation.py`（新增 tool_prepare_read_only）
  - `apps/control-api/src/agenttest/bootstrap/app.py`（保留 gateway 变量并传入 SuperAgentConversation）
- 数据库/API/配置变化：无（API 契约向后兼容）
- 验证记录：
  - `ruff check`：通过（0 errors）
  - `pytest apps/control-api/tests/unit/test_agent/`：27 passed
  - `pytest apps/control-api/tests/ --ignore=tests/architecture`：124 passed、5 skipped
  - `pytest apps/control-api/tests/contract/`：100 passed
  - 1 个既有失败（migrations）与本次无关
  - 3 个既有架构违规（adapters/platform.py）与本次无关
- 遗留问题：
  - `request_stream()` 当前为模拟流式（句子级分块），真正 SSE 推送需 Temporal 回调基础设施
  - PydanticAI 0.7.2 不支持异步 human-in-the-loop tool approval，WRITE 操作继续走 Orchestrator
- 下一步：
  - Phase 4：类型化工具参数（从 input_model 生成精确 JSON Schema）✅ 已于 TASK-20260701-005 交付

### TASK-20260701-005：PydanticAI Phase 4 — 类型化工具参数

- 状态：已完成
- 开始日期：2026-07-01
- 完成日期：2026-07-01
- 负责人：AI
- 关联需求：PRD 8.3、8.6、12、14、15
- 目标：将 SubAgent READ 工具参数从 `**kwargs`（空 JSON Schema `{}`）升级为从 `input_model` 生成的精确 JSON Schema，让 LLM 在 tool calling 时看到完整字段类型、约束和描述。
- 实施计划：Phase4-类型化工具参数
- 实现摘要：
  - 重构 `_make_read_tool()` — 从返回裸函数改为返回 `Tool` 对象（含自定义 `FunctionSchema`，`parameters_json_schema` 来自 `input_model_cls.model_json_schema()`）
  - 新增 imports：`Tool`（pydantic_ai.tools）、`FunctionSchema`（pydantic_ai._function_schema）、`SchemaValidator`/`core_schema`（pydantic_core）
  - 注入 `tool_prepare_read_only` 作为 Tool.prepare 回调（运行时条件注册：platform_gateway 不可用时工具不注册）
  - 13 个 READ 工具均获得精确 Schema，例如 `agents.list` 可见 `query: string|null`、`agents.analyze_endpoint` 可见 `agent_version_id: string (required)` + `probe_input: object|null`
- 变更文件：
  - `apps/control-api/src/agenttest/modules/test_agent/application/super_agent.py`（重构 _make_read_tool / _build_domain_read_tools，+36 行 / -9 行）
- 数据库/API/配置变化：无
- 验证记录：
  - `ruff check`：通过（0 errors）
  - `pytest apps/control-api/tests/unit/test_agent/`：27 passed
  - `pytest apps/control-api/tests/ --ignore=tests/architecture`：579 passed、6 skipped
  - `pytest apps/control-api/tests/contract/`：100 passed
  - Tool JSON Schema 验证：`agents.list` → `{"query": ...}`、`agents.analyze_endpoint` → `{"agent_version_id": ..., "probe_input": ...}`、`reports.generate` → `{"run_id": ...}` 均通过
  - 1 个既有失败（migrations）与本次无关
- 遗留问题：
  - `Tool` 构造使用私有 API `pydantic_ai._function_schema.FunctionSchema`，升级 pydantic-ai 版本时需验证
- 下一步：
  - Phase 5：Worker 流式回调基础设施 ✅ 已于 TASK-20260701-006 交付

### TASK-20260701-006：PydanticAI Phase 5 — Worker 流式回调可选化

- 状态：已完成
- 开始日期：2026-07-01
- 完成日期：2026-07-01
- 负责人：AI
- 关联需求：PRD 8.3、8.6、12
- 目标：将 Worker 流式回调从必填改为可选，使 `TemporalModel.request_stream()` 能调用真实的 `model-streaming` Workflow（Worker 侧 SSE → Provider）。
- 实施计划：Phase5-流式回调可选化
- 实现摘要：
  - `activities.py`：`stream_model` 中回调改为 `payload.get("callback")` 可选，无回调时跳过 POST 只积累返回；回调异常改为 `pass` 降级（避免单次 HTTP 失败导致 Workflow 失败）
  - `ports.py`：`ModelInvoker.stream()` 的 `callback` 参数改为 `ModelStreamCallback | None = None`
  - `temporal_invoker.py`：`stream()` 方法条件化 payload — 仅当 `callback is not None` 时才注入 `payload["callback"]`
  - `temporal_model.py`：`request_stream()` 改为调用 `self._invoker.stream()` （不传 callback），Worker 侧 SSE → Provider，Agent 侧继续句子级分块
- 变更文件：
  - `workers/model-runner/src/agenttest_model_runner/activities.py`（callback 可选，+14 行 / -10 行）
  - `apps/control-api/src/agenttest/modules/model_configs/application/ports.py`（callback 类型可选，1 行）
  - `apps/control-api/src/agenttest/modules/model_configs/infrastructure/temporal_invoker.py`（条件化 payload，+2 行 / -1 行）
  - `apps/control-api/src/agenttest/modules/test_agent/application/temporal_model.py`（request_stream 用 stream()，+35 行 / -13 行）
- 数据库/API/配置变化：无
- 验证记录：
  - `ruff check`：通过（0 errors）
  - `pytest apps/control-api/tests/unit/test_agent/`：27 passed
  - `pytest apps/control-api/tests/ --ignore=tests/architecture`：579 passed、6 skipped
  - `pytest apps/control-api/tests/contract/`：100 passed
  - 既有失败（migrations + architecture）与本次无关
- 遗留问题：
  - 当前仍为句子级分块模拟流式（Agent 侧等完整响应），真正端到端流式需 Temporal Signals 或 in-process 回调监听器
  - Worker 回调 POST 异常改为 `pass` 会在有回调场景下静默丢失事件（仅影响无回调新路径）
- 下一步：
  - Phase 6：in-process 流式回调监听器 ✅ 已于 TASK-20260701-007 交付

### TASK-20260701-007：PydanticAI Phase 6 — In-Process 流式回调监听器

- 状态：已完成
- 开始日期：2026-07-01
- 完成日期：2026-07-01
- 负责人：AI
- 关联需求：PRD 8.3、8.6、12
- 目标：在 `TemporalModel.request_stream()` 中启动本地 HTTP server 作为 Worker 回调端点，Worker SSE chunk → HTTP POST → asyncio.Queue → `_get_event_iterator()`，实现真正的逐 token 流式输出。
- 实施计划：Phase6-InProcess流式回调监听器
- 实现摘要：
  - 新增 `_handle_callback_stream()` 函数：零依赖 asyncio TCP 回调处理，最小 HTTP 解析（~50 行）
  - 重写 `request_stream()` 方法（~120 行→~95 行）：启动本地 asyncio TCP server（127.0.0.1:0），传入 `ModelStreamCallback`，后台 `ensure_future` 执行 `invoker.stream()`，`_IncrementalStreamedResponse` 从 asyncio.Queue 实时消费 chunk
  - 降级回退：如果流式 chunks 为空（回调链路失败），回退到 `_split_sentences()` 句子级分块
  - Tool calls 从最终结果解析（Worker adapter 不产出 tool_call delta）
- 变更文件：
  - `apps/control-api/src/agenttest/modules/test_agent/application/temporal_model.py`（+150 行 / -38 行）
- 数据库/API/配置变化：无
- 验证记录：
  - `ruff check`：通过（0 errors）
  - `pytest apps/control-api/tests/unit/test_agent/`：27 passed
  - `pytest apps/control-api/tests/contract/`：100 passed
- 遗留问题：
  - 生产部署需确保 Worker 能路由到 Control API 的 callback 端口（开发环境 127.0.0.1 同机可达）
  - 未覆盖：Worker adapter 的 tool_call delta 流式（当前 adapter 只产出 text content）
- 下一步：
  - 可考虑：流式 Worker 超时/取消信号传播、callback token 轮换

---

## 4. 已完成任务

### TASK-20260701-002：PydanticAI SubAgent 架构重构（Phase 1）

- 状态：已完成
- 开始日期：2026-07-01
- 完成日期：2026-07-01
- 负责人：AI
- 关联需求：PRD 8.3、8.6、12、14、15
- 关联架构：技术架构规范 4、5、7、8、9
- 目标：将自研 SubAgent 路由/规划/编排层迁移到 PydanticAI 框架，利用 Agent delegation、Structured Output、DI、Tool Approval 等成熟能力，代码量减少 ~27%。
- 实施计划：`docs/superpowers/plans/2026-07-01-pydanticai-subagent-refactor.md`
- 实现摘要：
  - 安装 pydantic-ai 0.7.2 及其 60 个传递依赖
  - 创建 `context.py`：PydanticAI DI 容器（OrchestrationContext）
  - 重写 `sub_agents.py`：删除 SubAgentRouter 类（~80行），保留 9 个 SubAgent prompt + 注册表（393→293 行）
  - 新建 `super_agent.py`：SubAgentRouter 静态路由 + plan_actions_for_subagent 领域特化规划 + ActionIntent 输出类型（237 行）
  - 新建 `confirmation.py`：风险驱动确认处理器（ConfirmationHandler + READ/DRAFT_WRITE/HIGH_IMPACT 三级模型，69 行）
  - 重写 `conversation.py`：删除 SubAgentRouter 实例依赖，改用 super_agent 模块静态方法（358→240 行）
  - 更新 `bootstrap/app.py`：移除 SubAgentRouter 导入和构造参数
  - 修复 `_route_and_plan()` 路由为空时未回退到 `_plan_actions()` 的 bug
- 变更文件：
  - `apps/control-api/pyproject.toml`（依赖）
  - `apps/control-api/src/agenttest/modules/test_agent/application/context.py`（新增，34 行）
  - `apps/control-api/src/agenttest/modules/test_agent/application/sub_agents.py`（重写，393→293 行）
  - `apps/control-api/src/agenttest/modules/test_agent/application/super_agent.py`（新增，235 行）
  - `apps/control-api/src/agenttest/modules/test_agent/application/confirmation.py`（新增，69 行）
  - `apps/control-api/src/agenttest/modules/test_agent/application/conversation.py`（重写，358→240 行）
  - `apps/control-api/src/agenttest/bootstrap/app.py`（移除 SubAgentRouter 依赖）
  - `uv.lock`（依赖锁定）
- 数据库/API/配置变化：无（纯应用层重构，API 签名不变）
- 验证记录：
  - `ruff check apps/control-api`：通过
  - `pytest apps/control-api/tests/ --ignore=apps/control-api/tests/architecture`：573 passed、6 skipped（需 PostgreSQL）
  - 2 个既有失败（架构边界 platform.py + 迁移版本号），与本次重构无关
  - 代码量：~1170 → ~870 行（-300 行，-25.6%）
- 遗留问题：无
- 下一步：Phase 2 — 将 PydanticAI Agent 实例接入 Temporal Model Runner（真正的 Agent delegation + tool calling）

### TASK-20260625-DOC-001：完成产品需求文档

- 状态：已完成
- 完成日期：2026-06-25
- 实现摘要：
  - 定义通用 Agent 自动化测试与安全评估平台。
  - 明确画布 Agent 首个落地插件。
  - 定义登录、用户、项目隔离、双浏览器引擎、安全和 UI/UX。
- 变更文件：
  - `docs/Agent测试平台产品需求文档-PRD.md`
- 验证记录：
  - 文档占位符和章节一致性检查：通过
- 遗留问题：
  - 尚未产出页面原型和详细实施计划
- 下一步：
  - 编写 MVP 实施计划

### TASK-20260625-DOC-002：完成技术架构与开发规范

- 状态：已完成
- 完成日期：2026-06-25
- 实现摘要：
  - 确定模块化单体控制面、Temporal、独立 Worker 和插件 SDK。
  - 定义前端、后端、数据库、目录、契约、测试和 CI/CD 规范。
  - 定义开源框架最新稳定版核对、锁定和升级规则。
- 变更文件：
  - `docs/Agent测试平台技术架构与开发规范.md`
- 验证记录：
  - 架构章节、依赖方向和占位符检查：通过
- 遗留问题：
  - 尚未建立 ADR 文件和工程脚手架
- 下一步：
  - 依据架构规范编写实施计划

### TASK-20260625-DOC-003：建立 AI 开发协作与记录机制

- 状态：已完成
- 完成日期：2026-06-25
- 实现摘要：
  - 建立 AI 开发前、中、后强制工作协议。
  - 建立里程碑、任务、验证、遗留问题和交接台账。
- 变更文件：
  - `AGENTS.md`
  - `docs/开发进度与变更记录.md`
- 验证记录：
  - 文档引用和流程闭环检查：通过
- 遗留问题：
  - 当前目录尚未初始化 Git 仓库
- 下一步：
  - 创建实施计划并初始化工程

### TASK-20260625-004：初始化并关联 GitHub 仓库

- 状态：已完成
- 完成日期：2026-06-25
- 关联需求：开发协作与版本管理
- 关联架构：技术架构规范第 5、17 章
- 实现摘要：
  - 初始化本地 Git 仓库和 `main` 分支。
  - 配置提交身份 `xiyao1203 / jasonxtry@126.com`。
  - 关联 SSH 远端 `git@github.com:xiyao1203/warmy-agent-test.git`。
  - 验证 GitHub SSH 认证和远端历史。
  - 确认本地 `main` 与远端 `main` 正常关联。
- 变更文件：
  - `.gitignore`
  - `docs/开发进度与变更记录.md`
- 验证记录：
  - `git remote -v`：远端地址正确
  - `git ls-remote --symref origin HEAD`：SSH 认证成功，默认分支为 `main`
  - `git fetch origin main`：成功
- 遗留问题：
  - 无
- 下一步：
  - 编写 MVP 实施计划

### TASK-20260625-005：优化 Codex 开发上下文与交接机制

- 状态：已完成
- 完成日期：2026-06-25
- 关联需求：AI 持续开发与上下文恢复
- 关联架构：根目录 `AGENTS.md`、技术架构规范全文
- 实现摘要：
  - 为 Codex 增加快速启动、上下文恢复、Git 行为和子目录指令规则。
  - 建立单一 `当前任务` 文件，避免从长进度台账推测工作范围。
  - 增加 Codex 开发、续接、评审和交接提示词。
  - 增加项目 README 和 Codex 指令加载验证方法。
  - 明确未来前端、后端、Worker 和插件目录的局部 `AGENTS.md` 策略。
- 变更文件：
  - `.gitignore`
  - `AGENTS.md`
  - `README.md`
  - `docs/Codex开发指南.md`
  - `docs/当前任务.md`
  - `docs/开发进度与变更记录.md`
- 数据库/API/配置变化：
  - 无
- 验证记录：
  - 文档交叉引用检查：通过
  - 根 `AGENTS.md` 大小低于 Codex 默认 32 KiB 项目指令上限：通过
  - Codex 官方 `AGENTS.md` 发现与覆盖机制核对：通过
- 遗留问题：
  - 应用代码初始化后需要为 `apps/web`、`apps/control-api`、`workers` 和 `plugins` 增加局部指令
- 下一步：
  - 执行 TASK-20260625-001，编写 MVP 实施计划

### TASK-20260625-001：编写 MVP 实施计划

- 状态：已完成
- 完成日期：2026-06-25
- 关联需求：PRD 第 17–19 章
- 关联架构：技术架构规范全文
- 实现摘要：
  - 将 MVP 拆分为 M0–M6 七个可独立验收的阶段。
  - 明确每个阶段的范围、依赖、交付物和退出标准。
  - 为 M0 工程基线和 M1 身份项目平台编写 15 个详细实施任务。
  - 每个详细任务包含文件路径、失败测试、实现要求、验证命令和提交点。
  - 覆盖 Monorepo、CI、OpenAPI、登录、超级管理员、项目隔离、审计和前端 E2E。
- 变更文件：
  - `docs/superpowers/plans/2026-06-25-mvp-roadmap.md`
  - `docs/superpowers/plans/2026-06-25-m1-platform-foundation.md`
  - `docs/当前任务.md`
  - `docs/开发进度与变更记录.md`
- 数据库/API/配置变化：
  - 无；本任务仅产出计划
- 验证记录：
  - MVP 必做功能覆盖检查：通过
  - 计划占位符扫描：通过
  - 任务依赖、目录和类型一致性检查：通过
- 遗留问题：
  - M2–M6 的详细文件级计划将在各阶段开始前依据实际代码生成
- 下一步：
  - 执行 TASK-20260625-002，按详细计划 Task 1 初始化 Monorepo

### TASK-20260625-002：初始化 Monorepo 与质量门禁

- 状态：已完成
- 完成日期：2026-06-25
- 关联需求：PRD 第 12、17 章
- 关联架构：技术架构规范第 5、6、7、17 章
- 目标：建立 Web、Control API、Packages、基础设施和 CI 工程基线
- 实现摘要：
  - 建立 pnpm/uv Monorepo 工程基线，包含 Workspace 和 Lockfile。
  - 搭建本地基础设施 Compose 配置（PostgreSQL、Redis、Temporal）。
  - 搭建 FastAPI 控制面脚手架（App Factory、Settings、Health API、Problem Details）。
  - 搭建 Next.js 平台壳（设计 Token、AppShell 组件、Vitest 测试）。
  - 开发分支 `feature/m1-platform-foundation` 已完成并合并至 `main`。
- 合并统计：
  - 开发分支：`feature/m1-platform-foundation`
  - 合并 Commit：`a5faf19`
  - 合并提交数：7 个（含合并提交和 `.gitignore` 更新）
  - 变更文件数：50 个
  - 新增代码行数：约 7,162 行（含合并调整）
  - 新增主要模块：`apps/web/`、`apps/control-api/`、`apps/admin-cli/`、`infra/compose/`、`scripts/`
- 变更文件（主要）：
  - `.editorconfig`、`.gitignore`、`Makefile`、`package.json`、`pnpm-workspace.yaml`
  - `apps/web/`（Next.js 前端壳，共 17 个文件）
  - `apps/control-api/`（FastAPI 后端，共 12 个文件）
  - `apps/admin-cli/pyproject.toml`
  - `packages/generated-api-client/.gitkeep`
  - `infra/compose/compose.yaml`、`infra/compose/.env.example`
  - `scripts/`（3 个辅助脚本）
  - `docs/runbooks/local-development.md`
  - `pnpm-lock.yaml`、`uv.lock`
- 数据库/API/配置变化：
  - 新增 `infra/compose/compose.yaml`（PostgreSQL 16、Redis 7、Temporal）
  - 新增 FastAPI Health API 端点 `/health/live`、`/health/ready`
- 验证记录：
  - `bash scripts/check_workspace.sh`：通过
  - FastAPI Health/Settings 单元/契约测试（2 个）：通过
  - `ruff check apps/control-api`：通过
  - `mypy apps/control-api/src`：通过
  - Web Vitest 组件测试（1 个）：通过
  - Web ESLint、TypeScript 检查：通过
  - Next.js Production Build：通过
  - Compose YAML 和 Python 脚本静态检查：通过
- 遗留问题：
  - 当前环境没有 Docker，Compose 服务启动和健康检查尚未实际执行
  - ADR-001 至 ADR-008 尚未创建
  - 详细计划 Task 5（OpenAPI Client 生成）和 Task 6–7 尚未完成
- 下一步：
  - 执行 TASK-20260625-003：实现登录、用户与项目权限
  - 补充 ADR 文件
  - 完成 OpenAPI TypeScript Client 生成与漂移检查

---

## 8. 已取消任务

当前无已取消任务。

---

## 9. 已知问题与技术债

| 编号 | 问题 | 风险 | 计划处理 |
|---|---|---|---|
| TD-001 | M1 PostgreSQL 空库迁移、降级再升级和真实约束测试未执行 | 迁移可能存在隐含问题 | 在有 Docker 的环境中运行迁移验证 |
| TD-002 | M1 项目隔离和审计原子回滚的 PostgreSQL 集成测试未执行 | 隔离策略可能有边界遗漏 | 在有 Docker 的环境中运行集成测试 |
| TD-003 | M1 Docker Compose 启动和健康检查未实际执行 | 基础设施配置可能有问题 | 在有 Docker 的环境中验证 |
| TD-004 | M1 GitHub Actions 工作流尚未推送触发 | CI 配置可能有语法或环境问题 | 推送后验证 CI 运行结果 |
| TD-005 | M1 登录速率限制未实现 | 暴力破解风险 | M2 或安全加固阶段补充 |
| TD-006 | FastAPI TestClient 产生 1 条 Starlette/httpx 弃用警告 | 无功能影响，后续可能升级 | 后续依赖维护时处理 |

---

## 10. 关键决策记录

| 日期 | 决策 |
|---|---|
| 2026-06-25 | 产品采用 Web 应用，不开发桌面端 |
| 2026-06-25 | 数据采用项目内共享、项目间隔离 |
| 2026-06-25 | 只有超级管理员可以 CRUD 用户 |
| 2026-06-25 | 前端采用 Linear / Vercel 式高密度工作台 |
| 2026-06-25 | 控制面采用模块化单体，执行器采用独立 Worker |
| 2026-06-25 | 长任务统一使用 Temporal |
| 2026-06-25 | Playwright 负责回归，Browser Harness 负责探索 |
| 2026-06-25 | 开源框架接入时使用最新稳定版并锁定版本 |

---

## 11. 下一步建议

1. 编写 M2 详细实施计划（Agent、Dataset、TestCase、TestPlan 领域模型、迁移、API 和 UI）。
2. 在有 Docker 的环境中补验 M1：PostgreSQL 迁移、项目隔离、审计回滚和 Compose 启动。
3. 推送触发 GitHub Actions 并验证 CI 全部任务通过。
4. M1 环境验证通过后标记 M1 为已完成。

### TASK-20260627-005：环境快照 API 完善

- 状态：已完成
- 分支：codex/feature/phase3-environments
- Commit：998937f
- 变更文件：
  - `apps/control-api/src/agenttest/modules/environments/domain/repositories.py` — 新增 `get_by_id_and_project` 接口
  - `apps/control-api/src/agenttest/modules/environments/infrastructure/persistence/repositories.py` — 实现 project_id 过滤
  - `apps/control-api/src/agenttest/modules/environments/api/snapshots.py` — 修复 repo 调用 + 类型安全
  - `apps/control-api/tests/unit/environments/test_snapshots.py` — 6 个快照领域单元测试（新增）
- 验证：ruff ✅ / mypy ✅ / 148 tests ✅（+6 新增）
- 遗留：test_environment_domain.py:62 有 1 个 mypy dict variance 错误（现有，非本次引入）

### TASK-20260629-008：模型配置默认取消与删除错误提示优化

- 状态：已完成
- 完成日期：2026-06-29
- 分支：`main`
- 关联需求：项目模型配置管理
- 目标：修复「仅有一个模型时无法删除（因为被设为默认且不支持取消）」的问题，同时优化前端删除失败的 409 错误提示。
- 实现摘要：
  - 后端新增 `DELETE /api/v1/projects/{project_id}/model-defaults/{purpose}` 端点，支持取消指定用途的默认模型绑定。
  - 领域层协议、持久化仓库、应用服务同步补齐 `delete_default`/`clear_default` 方法。
  - 前端 `api.ts` 新增 `clearModelDefault` 函数，`model-config-screen.tsx` 在 `<select>` 选择"尚未配置"时调用取消接口（原为静默返回）。
  - 前端 `model-config-list.tsx` 删除对话框新增错误状态展示，409 冲突时显示后端错误详情提示用户先取消默认绑定。
- 变更文件：
  - `apps/control-api/src/agenttest/modules/model_configs/domain/repositories.py`
  - `apps/control-api/src/agenttest/modules/model_configs/infrastructure/persistence/repositories.py`
  - `apps/control-api/src/agenttest/modules/model_configs/application/service.py`
  - `apps/control-api/src/agenttest/modules/model_configs/api/router.py`
  - `apps/web/src/features/model-configs/api.ts`
  - `apps/web/src/features/model-configs/model-config-screen.tsx`
  - `apps/web/src/features/model-configs/model-config-list.tsx`
- 验证记录：
  - 后端 Router import: 通过
  - `curl DELETE /model-defaults/{purpose}`：204 ✅
  - `curl DELETE /model-configs/{id}`（取消默认后）：204 ✅
  - 前端 TypeScript `tsc --noEmit`：通过（0 errors）
- 遗留问题：无
- 下一步：无

## PRD 全量功能深化实施计划（2026-06-29）

计划文件：`docs/superpowers/plans/2026-06-29-prd-full-implementation-plan.md`

基于 2026-06-28 PRD 差距分析，制定 7 阶段详细实施计划，覆盖结果工作台、实验对比、人工审核、测试计划、Trace 可观测性、对话 Agent 和报告导出。

总预计工期：12-13 天

### 阶段一：结果工作台优化（2 天）
| 任务 | 预计工期 | 状态 |
|------|----------|------|
| Trace 树形可视化组件 | 1 天 | 待开始 |
| 结果工作台三栏布局 | 0.5 天 | 待开始 |
| 产物预览集成 | 0.5 天 | 待开始 |

### 阶段二：实验对比统计增强（1.5 天）
| 任务 | 预计工期 | 状态 |
|------|----------|------|
| 统计分析后端 API | 0.5 天 | 待开始 |
| 对比 UI 增强 | 1 天 | 待开始 |

### 阶段三：人工审核深度集成（2 天）
| 任务 | 预计工期 | 状态 |
|------|----------|------|
| 低置信度自动收集逻辑 | 0.5 天 | 待开始 |
| A/B 偏好选择 UI | 1 天 | 待开始 |
| Rubric 多维评分配置 | 0.5 天 | 待开始 |
| 多人审核一致性统计 | 0.5 天 | 待开始 |

### 阶段四：测试计划高级配置（1.5 天）
| 任务 | 预计工期 | 状态 |
|------|----------|------|
| 评分器权重配置 UI | 0.5 天 | 待开始 |
| 阈值校验逻辑 | 0.5 天 | 待开始 |
| 成本预算控制 | 0.5 天 | 待开始 |

### 阶段五：Trace 可观测性增强（2 天）
| 任务 | 预计工期 | 状态 |
|------|----------|------|
| OpenTelemetry 集成 | 1 天 | 待开始 |
| 时间轴展示组件 | 0.5 天 | ✅ 已完成 |
| 版本轨迹对比 | 0.5 天 | ✅ 已完成 |

### 阶段六：对话 Agent 智能协作（1.5 天）
| 任务 | 预计工期 | 状态 |
|------|----------|------|
| 智能推荐功能 | 0.5 天 | ✅ 已完成 |
| 成本估算卡片 | 0.5 天 | ✅ 已完成 |
| 失败→回归转换 | 0.5 天 | ✅ 已完成 |

### 阶段七：报告导出标准化（1.5 天）
| 任务 | 预计工期 | 状态 |
|------|----------|------|
| 报告生成器 | 1 天 | ✅ 已完成 |
| CI 集成 | 0.5 天 | ✅ 已完成 |
