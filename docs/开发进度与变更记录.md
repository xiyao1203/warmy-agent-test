# Agent 测试平台开发进度与变更记录

本文件是项目开发状态的唯一人工可读台账。所有 AI Agent 和开发人员在开始和完成任务时都必须更新本文件。

更新规则见根目录 `AGENTS.md`。

---

## 1. 项目状态

| 项目项 | 当前状态 |
|---|---|
| 当前阶段 | M3 执行闭环（M4 已完成） |
| 当前里程碑 | M3：通用 HTTP Agent 测试闭环 + Artifact 产物 |
| 总体状态 | 开发中 |
| 最后更新 | 2026-06-27 |
| 当前代码状态 | M3 首个 Run API + Worker + 运行中心纵向切片已完成，M4 画布插件（断言引擎 + 多模态评分 + 注册表）已交付，Artifact 产物上传/下载开发中 |
| 当前阻塞 | M1/M3 真实环境依赖验证待补（PostgreSQL/Docker/Temporal/CI） |

---

## 2. 里程碑

| 里程碑 | 目标 | 状态 | 完成度 |
|---|---|---|---:|
| M0 开发准备 | PRD、架构、开发规范和实施计划 | 已完成 | 100% |
| M1 平台基础 | Monorepo、登录、用户、项目和权限 | 待验证 | 90% |
| M2 测试资产 | Agent、数据集、用例和测试计划 | 已完成 | 100% |
| M3 执行闭环 | Temporal、API Runner、运行与 Trace | 进行中 | 78% |
| M4 画布测试 | Canvas 插件、结构断言和多模态评测 | 已完成 | 100% |
| M5 浏览器测试 | Playwright 与 Browser Harness | 待开始 | 0% |
| M6 安全与门禁 | Promptfoo、安全策略和 CI/CD | 待开始 | 0% |

完成度是进度参考，不能代替验收标准。

---

## 3. 当前进行中

### TASK-20260626-001：M3 通用 HTTP Agent 测试闭环

- 状态：进行中
- 开始日期：2026-06-26
- 负责人：AI
- 开发分支：`codex/feature/m3-ui-polish`（方案 1 UI 打磨分支，承接 `codex/feature/m3-api-runner` 的 M3 工作）
- 关联需求：PRD 第 7.3、8.6、8.8、8.9、15、17.1、19.3 章
- 关联架构：技术架构第 4.2、4.3、8.4、8.5、9.2、9.3、12、13、15.4 章及 ADR-0002
- 目标：交付基于已发布测试计划的 Run/RunCase 创建、执行、进度、取消、结果与 Trace 前后端闭环。
- 计划修改：
  - `apps/control-api/`
  - `workers/api-runner/`
  - `apps/web/`
  - `packages/generated-api-client/`
  - `docs/api/`、`docs/security/dependencies/`
- 验收方式：后端单元/契约/隔离/迁移/架构测试，Workflow replay/重试/超时/取消，前端组件/E2E/Lint/类型检查/Build，OpenAPI 漂移检查。
- 当前进展：
  - 已读取当前任务、开发进度、PRD、技术架构、ADR-0002 和 MVP 路线图。
  - 已确认 M2 完成且 `main` 与 `origin/main` 同步、工作区干净。
  - 已创建开发分支 `codex/feature/m3-api-runner`。
  - 已创建详细计划 `docs/superpowers/plans/2026-06-26-m3-api-runner.md`。
  - 已完成 Run/RunCase/RunEvent 领域模型、迁移、项目隔离约束和幂等创建/取消/查询 API。
  - 已完成基础事件流端点、RunCase/Trace 快照查询和 OpenAPI/TypeScript Client 生成。
  - 已完成 `workers/api-runner` 包、Generic HTTP Agent Adapter、Activity 与 Temporal Workflow 骨架。
  - 已完成控制面结果回写应用服务和仓储保存逻辑，可按 Worker DTO 更新 RunCase、聚合 Run 终态，并保护终态幂等。
  - 已完成受 `X-Internal-Token` 保护的内部结果回写 API，并同步 OpenAPI、TypeScript Client 和 Runs API 文档。
  - 已完成真实 Temporal `RunOrchestrator` Adapter 代码路径：配置 Temporal 地址后懒连接、启动 `RunWorkflow`、传递项目/回写配置并支持取消 signal；未配置时仍使用本地编排器。
  - 已完成 Worker 内部结果回写 Activity 和 Workflow 接入，Worker 通过 HTTP 调控制面内部回写端点，不连接业务数据库。
  - 已完成 Worker JSON、JUnit XML 和 HTML 报告产物生成；已覆盖 100 条 case 报告生成和 Workflow 100 case 合约。
  - 已完成前端运行中心和运行详情页面；运行详情接入 SSE 快照刷新，断线后回退查询刷新，左侧导航新增“运行中心”，Agent 流式对话三栏工作区保持不变。
  - 已按方案 1 管理台风格完成前端运行体验打磨：应用壳顶部搜索/帮助/通知/用户区、项目导航命名、运行中心统计卡、搜索与状态筛选、匹配空状态、运行详情右侧执行摘要和实时刷新提示。
  - 已修复资产列表操作列挤压问题：Agent、数据集、测试计划列表统一使用不换行的横向表格操作区，查看/删除按钮保持单行且具备完整无障碍名称。
  - 已优化资产列表表头与字段布局：Agent、数据集、测试计划列表改为固定列宽、中文语义表头、浅色表头背景、行 hover 和名称/描述截断，避免宽屏下字段漂移和大面积空白。
  - 已微调资产列表首列表头对齐：表头文字与行内名称文字起点对齐，避开图标列造成的视觉错位。
  - 已重新收紧资产列表列组：表格改为内容宽度，短字段列定宽居中，操作列居中，避免类型/时间/操作字段被拉散到页面中间。
  - 已补前端运行详情 SSE 退避重连：连接异常后按 1s、2s、4s 最多 3 次显式重连，超过次数后回退查询轮询。
  - 已新增 `docs/api/runs.md` 和 `docs/security/dependencies/temporal-python-sdk.md`。
  - 当前代码已具备本地 Fake 编排纵向切片、真实 Temporal Adapter 代码路径、结果回写、报告和 100 用例逻辑验收；真实 Temporal/PostgreSQL/Docker 环境与 Artifact 上传待补。
- 本轮验证记录（2026-06-26）：
  - `.venv/bin/python -m pytest apps/control-api/tests/architecture apps/control-api/tests/contract/test_runs_api.py apps/control-api/tests/unit/runs apps/control-api/tests/integration/test_run_constraints.py workers/api-runner/tests -q`：20 passed，3 warnings。
  - `.venv/bin/python -m pytest apps/control-api/tests/unit/runs/test_run_result_handler.py -q`：4 passed，1 warning。
  - 补结果回写后复验 `.venv/bin/python -m pytest apps/control-api/tests/architecture apps/control-api/tests/contract/test_runs_api.py apps/control-api/tests/unit/runs apps/control-api/tests/integration/test_run_constraints.py workers/api-runner/tests -q`：24 passed，4 warnings。
  - 补内部结果回写 API 与前端 SSE 后复验 `.venv/bin/python -m pytest apps/control-api/tests/architecture apps/control-api/tests/contract/test_runs_api.py apps/control-api/tests/unit/runs apps/control-api/tests/integration/test_run_constraints.py workers/api-runner/tests -q`：26 passed，4 warnings。
  - `.venv/bin/python -m pytest apps/control-api/tests/unit/runs/test_temporal_orchestrator.py workers/api-runner/tests/test_callback.py -q`：4 passed，1 warning。
  - 补 Temporal Adapter 与 Worker 回写 Activity 后复验 `.venv/bin/python -m pytest apps/control-api/tests/architecture apps/control-api/tests/contract/test_runs_api.py apps/control-api/tests/unit/runs apps/control-api/tests/integration/test_run_constraints.py workers/api-runner/tests -q`：32 passed，5 warnings。
  - `.venv/bin/python -m pytest workers/api-runner/tests/test_reports.py -q`：2 passed。
  - `.venv/bin/python -m pytest workers/api-runner/tests -q`：12 passed。
  - 补报告产物与 100 case 验收后复验 `.venv/bin/python -m pytest apps/control-api/tests/architecture apps/control-api/tests/contract/test_runs_api.py apps/control-api/tests/unit/runs apps/control-api/tests/integration/test_run_constraints.py workers/api-runner/tests -q`：34 passed，5 warnings。
  - `.venv/bin/python -m ruff check apps/control-api/src/agenttest/bootstrap/run_source.py apps/control-api/src/agenttest/modules/runs workers/api-runner`：通过。
  - `.venv/bin/python -m ruff check apps/control-api/src/agenttest/bootstrap/settings.py apps/control-api/src/agenttest/bootstrap/app.py apps/control-api/src/agenttest/modules/runs apps/control-api/tests/contract/test_runs_api.py apps/control-api/tests/unit/runs workers/api-runner`：通过。
  - `.venv/bin/python -m ruff check apps/control-api/src/agenttest/bootstrap/settings.py apps/control-api/src/agenttest/bootstrap/app.py apps/control-api/src/agenttest/modules/runs apps/control-api/tests/unit/runs apps/control-api/tests/contract/test_runs_api.py workers/api-runner`：通过。
  - `.venv/bin/python -m mypy apps/control-api/src apps/admin-cli/src workers/api-runner/src`：通过，175 个源文件无问题。
  - `pnpm --filter @warmy/web test -- src/features/runs/tests/run-center.test.tsx`：13 个测试文件、41 个测试通过。
  - 补前端 SSE 后复验 `pnpm --filter @warmy/web test -- src/features/runs/tests/run-center.test.tsx`：13 个测试文件、43 个测试通过。
  - `pnpm --filter @warmy/web lint && pnpm --filter @warmy/web typecheck && pnpm --filter @warmy/web build`：通过。
  - `pnpm --filter @warmy/web build`：通过。
  - `scripts/export_openapi.py` + `pnpm --filter @warmy/generated-api-client generate` 重复生成校验：OpenAPI/client generation stable。
  - `pnpm --filter @warmy/generated-api-client test && pnpm --filter @warmy/generated-api-client typecheck`：通过。
  - `pnpm --filter @warmy/web test -- src/features/runs/tests/run-center.test.tsx && pnpm --filter @warmy/web lint && pnpm --filter @warmy/web typecheck && pnpm --filter @warmy/generated-api-client test && pnpm --filter @warmy/generated-api-client typecheck`：通过。
  - `pnpm --filter @warmy/web lint && pnpm --filter @warmy/web test && pnpm --filter @warmy/web typecheck && pnpm --filter @warmy/generated-api-client test && pnpm --filter @warmy/generated-api-client typecheck`：通过。
  - 方案 1 前端运行体验打磨后复验 `pnpm --filter @warmy/web test -- src/features/runs/tests/run-center.test.tsx`：13 个测试文件、43 个测试通过。
  - 方案 1 前端运行体验打磨后复验 `pnpm --filter @warmy/web lint`：通过。
  - 方案 1 前端运行体验打磨后复验 `pnpm --filter @warmy/web typecheck`：通过。
  - 方案 1 前端运行体验打磨后复验 `pnpm --filter @warmy/web build`：通过。
  - 资产列表操作列修复后复验 `pnpm --filter @warmy/web test -- src/features/agents/tests/agent-list.test.tsx src/features/datasets/tests/dataset-list.test.tsx src/features/test-plans/tests/test-plan-list.test.tsx`：13 个测试文件、43 个测试通过。
  - 资产列表操作列修复后复验 `pnpm --filter @warmy/web lint`：通过。
  - 资产列表操作列修复后复验 `pnpm --filter @warmy/web typecheck`：通过。
  - 资产列表操作列修复后复验 `pnpm --filter @warmy/web build`：通过。
  - 资产列表表头与字段布局优化后复验 `pnpm --filter @warmy/web test -- src/features/agents/tests/agent-list.test.tsx src/features/datasets/tests/dataset-list.test.tsx src/features/test-plans/tests/test-plan-list.test.tsx`：13 个测试文件、43 个测试通过。
  - 资产列表表头与字段布局优化后复验 `pnpm --filter @warmy/web lint`：通过。
  - 资产列表表头与字段布局优化后复验 `pnpm --filter @warmy/web typecheck`：通过。
  - 资产列表表头与字段布局优化后复验 `pnpm --filter @warmy/web build`：通过。
  - 资产列表首列表头对齐微调后复验 `pnpm --filter @warmy/web test -- src/features/agents/tests/agent-list.test.tsx src/features/datasets/tests/dataset-list.test.tsx src/features/test-plans/tests/test-plan-list.test.tsx`：13 个测试文件、43 个测试通过。
  - 资产列表首列表头对齐微调后复验 `pnpm --filter @warmy/web lint`：通过。
  - 资产列表首列表头对齐微调后复验 `pnpm --filter @warmy/web typecheck`：通过。
  - 资产列表列组收紧后复验 `pnpm --filter @warmy/web test -- src/features/agents/tests/agent-list.test.tsx src/features/datasets/tests/dataset-list.test.tsx src/features/test-plans/tests/test-plan-list.test.tsx`：13 个测试文件、43 个测试通过。
  - 资产列表列组收紧后复验 `pnpm --filter @warmy/web lint`：通过。
  - 资产列表列组收紧后复验 `pnpm --filter @warmy/web typecheck`：通过。
  - 补 SSE 退避重连后复验 `pnpm --filter @warmy/web test -- src/features/runs/tests/run-center.test.tsx`：13 个测试文件、44 个测试通过。
  - 补 SSE 退避重连后复验 `pnpm --filter @warmy/web lint`：通过。
  - 补 SSE 退避重连后复验 `pnpm --filter @warmy/web typecheck`：通过。
  - 补 SSE 退避重连后复验 `pnpm --filter @warmy/web build`：通过。
  - `.venv/bin/python -m pytest apps/control-api/tests -q`：164 passed、4 skipped、3 failed。失败项为本任务前已存在基线问题：Secure Cookie 断言 2 项、离线迁移 PostgreSQL Schema SQL 断言 1 项。
- 未验证/遗留风险：
  - 未验证真实 PostgreSQL 空库迁移、上一版本升级、约束和索引。
  - 未验证 Docker Compose 与真实 Temporal Server 执行。
  - 未验证真实 Temporal Server 启动、Workflow replay history、真实取消清理、Artifact 上传。
  - 未完成真实 Artifact 上传与下载。
- 下一步：
  - 在真实 Temporal Server 环境验证 `RunOrchestrator` Adapter 和 Workflow 内部结果回写闭环。
  - 补真实 Artifact 上传/下载。
  - 在可用 Docker/PostgreSQL/Temporal 环境补齐 M1/M3 环境验证。

## 3A. 最近完成的里程碑

### TASK-20260625-006：M2 测试资产管理

- 状态：已完成
- 完成日期：2026-06-25
- 负责人：AI
- 关联需求：PRD Agent、数据集、用例和测试计划相关章节
- 关联架构：技术架构规范模块边界、不可变版本、项目隔离
- 目标：建立 Agent、Dataset、TestCase 和 TestPlan 的领域模型、API 和前端管理
- 验收方式：单元/集成/契约测试、迁移验证、前端组件测试、Lint、类型检查、构建
- 实现摘要：
- 当前进展：
  - 已完成 M2 详细实施计划（813 行）。
  - 已完成数据库迁移 0002_test_assets（agents、datasets、test_plans、environments 建表）。
  - 已完成 Agents、Dataset、TestPlan、EnvironmentTemplate 四大领域模型和应用层。
  - 新分支基线 133 个测试通过，4 个 PostgreSQL 测试跳过。
  - 当前子任务：详细计划 Task 6 测试资产 API 路由。
  - Agents API 已完成：9 个 CRUD/版本发布端点、项目访问适配、CSRF 和应用工厂注册。
  - Agents 契约与架构测试 8 个通过，Ruff 和 mypy 通过。
  - Dataset API 已完成：14 个数据集/版本/用例/导入导出端点、项目路径隔离、不可变版本、CSRF 和应用工厂注册。
  - Dataset 契约测试 6 个通过；Agents + Dataset + 架构测试 13 个通过；146 个源文件 mypy 和本次改动 Ruff 检查通过。
  - TestPlan API 已完成：9 个计划 CRUD/版本发布端点、项目路径隔离、不可变版本、CSRF 和应用工厂注册。
  - Agents + Dataset + TestPlan + 架构测试 17 个通过；148 个源文件 mypy 和本次改动 Ruff 检查通过。
  - EnvironmentTemplate API 已完成：5 个模板 CRUD 端点、项目路径隔离、CSRF 和应用工厂注册。
  - Task 6 四类测试资产 API 路由全部完成；联合契约与架构测试 21 个通过。
  - 全量后端回归 150 个通过、4 个 PostgreSQL 测试跳过；150 个源文件 mypy 和本次改动 Ruff 检查通过。
  - 仓库全量 Ruff 仍有 21 个基线既有问题（迁移换行、旧安全/领域测试导入与 UTC 写法），未在本任务中混入修复。
  - Task 7 已完成：OpenAPI 已包含四类测试资产 API，TypeScript Client 已重新生成，并新增 `docs/api/test-assets.md`。
  - OpenAPI 与客户端重复生成哈希一致；生成客户端 Vitest 1 个通过，TypeScript 类型检查通过，Lockfile 无漂移。
  - Task 8 已完成：Agent 列表、创建、详情、版本创建/编辑、发布确认、发布锁定和真实 API 查询接入。
  - Agent 组件测试 4 个通过；Web 全量 28 个测试、ESLint、TypeScript 和 Next.js Build 通过。
  - 为恢复 Web 质量门禁，清除了历史重复拼接的用户组件/测试，隔离 Vitest 与 Playwright 测试，并修复 Playwright Locator 类型错误。
  - Task 9 已完成：Dataset 列表、版本标签、TestCase 新增/编辑/删除、JSON 编辑、JSON/JSONL/CSV 导入导出、发布确认和发布后只读。
  - Dataset 组件测试 6 个通过；Web 全量 34 个测试、ESLint、TypeScript 和 Next.js Build 通过。
  - Task 10 已完成：TestPlan 列表、版本运行参数、已发布 Agent/Dataset 版本选择、环境模板引用、发布确认和发布后锁定。
  - TestPlan 组件测试 4 个通过；Web 全量 38 个测试、ESLint、TypeScript 和 Next.js Build 通过。
  - Task 11 已完成：左侧项目导航接入 Agents、Datasets、TestPlans，项目概览展示三类真实资产数量。
  - Agent 流式对话页预留的 `workspaceMode="agent"` 三栏结构保持不变。
  - Web 全量 38 个测试、ESLint、TypeScript 和 Next.js Build 通过。
  - 当前子任务：详细计划 Task 12 验收测试与文档。
  - 待实现：测试资产 E2E、README、最终 M2 验收和环境依赖验证记录。
  - 2026-06-25 补充：M2 四大模块全部代码已添加中文注释（按架构规范第 23 章）。

---

## 4. 待开始任务

当前无其他待开始任务。

---

## 5. 阻塞任务

当前无阻塞任务。

---

## 6. 待验证任务

### TASK-20260625-003：实现登录、用户与项目权限

- 状态：待验证
- 完成日期：2026-06-25（代码完成）
- 关联需求：PRD 第 8.1、16、19.1 章
- 关联架构：技术架构规范第 7.7、7.8、8.3、8.4 章
- 实现摘要：
  - M1 Plan Task 1–15 全部代码完成并合并至 `main`（最终合并 `7ed042b`）。
  - Monorepo 工程基线、FastAPI 控制面、Next.js 平台壳、Compose 基础设施。
  - 身份领域、Alembic 迁移、Argon2id 登录、服务端 Session、CSRF 防护。
  - 超级管理员 CLI、用户 CRUD API、最后管理员保护、密码重置和软删除。
  - 项目生命周期、成员管理、跨项目 404、移除即时失权。
  - 审计日志、敏感字段脱敏、Unit of Work 事务一致性。
  - OpenAPI 稳定导出、TypeScript Client 生成和漂移检查。
  - ADR-0001 至 ADR-0008、GitHub Actions CI 和 Dependabot。
  - 前端登录、平台壳、项目概览、用户管理（含编辑/删除）。
  - Playwright E2E 测试、Session 安全测试、API 文档和 README。
- 变更文件：
  - `apps/control-api/`（全后端实现）
  - `apps/admin-cli/`（超级管理员 CLI）
  - `apps/web/`（前端实现）
  - `packages/generated-api-client/`（API Client）
  - `infra/compose/`（基础设施）
  - `.github/workflows/`（CI）
  - `docs/`（ADR、API 文档、Runbook）
  - `README.md`、`Makefile`、根工程配置
- 数据库/API/配置变化：
  - 首个 Alembic 迁移：users、user_credentials、user_sessions、projects、project_members、audit.audit_logs
  - API：auth、system/users、projects、audit 端点
  - CI：python-quality、web-quality、contract、compose、security 工作流
- 验证记录：
  - 后端 66 个单元/契约测试通过，4 个 PostgreSQL 测试跳过。
  - Ruff、mypy、架构边界检查通过。
  - 前端 19 个组件测试通过，Lint、TypeCheck、Build 通过。
  - OpenAPI 漂移检查通过。
  - Session 安全测试已编写（Token 哈希、CSRF、登录不泄露、Session 撤销/过期）。
  - Playwright E2E 测试已编写（登录、用户管理、项目隔离）。
  - **未验证**：PostgreSQL 空库迁移、降级再升级、真实约束、项目隔离和审计回滚（当前环境无 Docker）。
  - **未验证**：Docker Compose 启动和健康检查（当前环境无 Docker）。
  - **未验证**：GitHub Actions 工作流实际运行（尚未推送触发）。
  - **未验证**：Playwright E2E 和前端全量验证（当前环境无 Node.js）。
- 遗留问题：
  - TD-001 至 TD-006 见已知问题与技术债。
- 下一步：
  - 在有 Docker 的环境中补验全部环境依赖测试。
  - 验证通过后标记为已完成。

---

## 7. 已完成任务

### TASK-20260625-DOC-001：完成产品需求文档

- 状态：已完成
- 完成日期：2026-06-25
- 实现摘要：
  - 定义通用 Agent 自动化测试与安全评估平台。
  - 明确画布 Agent 首个落地插件。
  - 定义登录、用户、项目隔离、双浏览器引擎、安全和 UI/UX。
- 变更文件：
  - `docs/Agent测试平台产品需求文档-PRD.md`
- 验证记录：
  - 文档占位符和章节一致性检查：通过
- 遗留问题：
  - 尚未产出页面原型和详细实施计划
- 下一步：
  - 编写 MVP 实施计划

### TASK-20260625-DOC-002：完成技术架构与开发规范

- 状态：已完成
- 完成日期：2026-06-25
- 实现摘要：
  - 确定模块化单体控制面、Temporal、独立 Worker 和插件 SDK。
  - 定义前端、后端、数据库、目录、契约、测试和 CI/CD 规范。
  - 定义开源框架最新稳定版核对、锁定和升级规则。
- 变更文件：
  - `docs/Agent测试平台技术架构与开发规范.md`
- 验证记录：
  - 架构章节、依赖方向和占位符检查：通过
- 遗留问题：
  - 尚未建立 ADR 文件和工程脚手架
- 下一步：
  - 依据架构规范编写实施计划

### TASK-20260625-DOC-003：建立 AI 开发协作与记录机制

- 状态：已完成
- 完成日期：2026-06-25
- 实现摘要：
  - 建立 AI 开发前、中、后强制工作协议。
  - 建立里程碑、任务、验证、遗留问题和交接台账。
- 变更文件：
  - `AGENTS.md`
  - `docs/开发进度与变更记录.md`
- 验证记录：
  - 文档引用和流程闭环检查：通过
- 遗留问题：
  - 当前目录尚未初始化 Git 仓库
- 下一步：
  - 创建实施计划并初始化工程

### TASK-20260625-004：初始化并关联 GitHub 仓库

- 状态：已完成
- 完成日期：2026-06-25
- 关联需求：开发协作与版本管理
- 关联架构：技术架构规范第 5、17 章
- 实现摘要：
  - 初始化本地 Git 仓库和 `main` 分支。
  - 配置提交身份 `xiyao1203 / jasonxtry@126.com`。
  - 关联 SSH 远端 `git@github.com:xiyao1203/warmy-agent-test.git`。
  - 验证 GitHub SSH 认证和远端历史。
  - 确认本地 `main` 与远端 `main` 正常关联。
- 变更文件：
  - `.gitignore`
  - `docs/开发进度与变更记录.md`
- 验证记录：
  - `git remote -v`：远端地址正确
  - `git ls-remote --symref origin HEAD`：SSH 认证成功，默认分支为 `main`
  - `git fetch origin main`：成功
- 遗留问题：
  - 无
- 下一步：
  - 编写 MVP 实施计划

### TASK-20260625-005：优化 Codex 开发上下文与交接机制

- 状态：已完成
- 完成日期：2026-06-25
- 关联需求：AI 持续开发与上下文恢复
- 关联架构：根目录 `AGENTS.md`、技术架构规范全文
- 实现摘要：
  - 为 Codex 增加快速启动、上下文恢复、Git 行为和子目录指令规则。
  - 建立单一 `当前任务` 文件，避免从长进度台账推测工作范围。
  - 增加 Codex 开发、续接、评审和交接提示词。
  - 增加项目 README 和 Codex 指令加载验证方法。
  - 明确未来前端、后端、Worker 和插件目录的局部 `AGENTS.md` 策略。
- 变更文件：
  - `.gitignore`
  - `AGENTS.md`
  - `README.md`
  - `docs/Codex开发指南.md`
  - `docs/当前任务.md`
  - `docs/开发进度与变更记录.md`
- 数据库/API/配置变化：
  - 无
- 验证记录：
  - 文档交叉引用检查：通过
  - 根 `AGENTS.md` 大小低于 Codex 默认 32 KiB 项目指令上限：通过
  - Codex 官方 `AGENTS.md` 发现与覆盖机制核对：通过
- 遗留问题：
  - 应用代码初始化后需要为 `apps/web`、`apps/control-api`、`workers` 和 `plugins` 增加局部指令
- 下一步：
  - 执行 TASK-20260625-001，编写 MVP 实施计划

### TASK-20260625-001：编写 MVP 实施计划

- 状态：已完成
- 完成日期：2026-06-25
- 关联需求：PRD 第 17–19 章
- 关联架构：技术架构规范全文
- 实现摘要：
  - 将 MVP 拆分为 M0–M6 七个可独立验收的阶段。
  - 明确每个阶段的范围、依赖、交付物和退出标准。
  - 为 M0 工程基线和 M1 身份项目平台编写 15 个详细实施任务。
  - 每个详细任务包含文件路径、失败测试、实现要求、验证命令和提交点。
  - 覆盖 Monorepo、CI、OpenAPI、登录、超级管理员、项目隔离、审计和前端 E2E。
- 变更文件：
  - `docs/superpowers/plans/2026-06-25-mvp-roadmap.md`
  - `docs/superpowers/plans/2026-06-25-m1-platform-foundation.md`
  - `docs/当前任务.md`
  - `docs/开发进度与变更记录.md`
- 数据库/API/配置变化：
  - 无；本任务仅产出计划
- 验证记录：
  - MVP 必做功能覆盖检查：通过
  - 计划占位符扫描：通过
  - 任务依赖、目录和类型一致性检查：通过
- 遗留问题：
  - M2–M6 的详细文件级计划将在各阶段开始前依据实际代码生成
- 下一步：
  - 执行 TASK-20260625-002，按详细计划 Task 1 初始化 Monorepo

### TASK-20260625-002：初始化 Monorepo 与质量门禁

- 状态：已完成
- 完成日期：2026-06-25
- 关联需求：PRD 第 12、17 章
- 关联架构：技术架构规范第 5、6、7、17 章
- 目标：建立 Web、Control API、Packages、基础设施和 CI 工程基线
- 实现摘要：
  - 建立 pnpm/uv Monorepo 工程基线，包含 Workspace 和 Lockfile。
  - 搭建本地基础设施 Compose 配置（PostgreSQL、Redis、Temporal）。
  - 搭建 FastAPI 控制面脚手架（App Factory、Settings、Health API、Problem Details）。
  - 搭建 Next.js 平台壳（设计 Token、AppShell 组件、Vitest 测试）。
  - 开发分支 `feature/m1-platform-foundation` 已完成并合并至 `main`。
- 合并统计：
  - 开发分支：`feature/m1-platform-foundation`
  - 合并 Commit：`a5faf19`
  - 合并提交数：7 个（含合并提交和 `.gitignore` 更新）
  - 变更文件数：50 个
  - 新增代码行数：约 7,162 行（含合并调整）
  - 新增主要模块：`apps/web/`、`apps/control-api/`、`apps/admin-cli/`、`infra/compose/`、`scripts/`
- 变更文件（主要）：
  - `.editorconfig`、`.gitignore`、`Makefile`、`package.json`、`pnpm-workspace.yaml`
  - `apps/web/`（Next.js 前端壳，共 17 个文件）
  - `apps/control-api/`（FastAPI 后端，共 12 个文件）
  - `apps/admin-cli/pyproject.toml`
  - `packages/generated-api-client/.gitkeep`
  - `infra/compose/compose.yaml`、`infra/compose/.env.example`
  - `scripts/`（3 个辅助脚本）
  - `docs/runbooks/local-development.md`
  - `pnpm-lock.yaml`、`uv.lock`
- 数据库/API/配置变化：
  - 新增 `infra/compose/compose.yaml`（PostgreSQL 16、Redis 7、Temporal）
  - 新增 FastAPI Health API 端点 `/health/live`、`/health/ready`
- 验证记录：
  - `bash scripts/check_workspace.sh`：通过
  - FastAPI Health/Settings 单元/契约测试（2 个）：通过
  - `ruff check apps/control-api`：通过
  - `mypy apps/control-api/src`：通过
  - Web Vitest 组件测试（1 个）：通过
  - Web ESLint、TypeScript 检查：通过
  - Next.js Production Build：通过
  - Compose YAML 和 Python 脚本静态检查：通过
- 遗留问题：
  - 当前环境没有 Docker，Compose 服务启动和健康检查尚未实际执行
  - ADR-001 至 ADR-008 尚未创建
  - 详细计划 Task 5（OpenAPI Client 生成）和 Task 6–7 尚未完成
- 下一步：
  - 执行 TASK-20260625-003：实现登录、用户与项目权限
  - 补充 ADR 文件
  - 完成 OpenAPI TypeScript Client 生成与漂移检查

---

## 8. 已取消任务

当前无已取消任务。

---

## 9. 已知问题与技术债

| 编号 | 问题 | 风险 | 计划处理 |
|---|---|---|---|
| TD-001 | M1 PostgreSQL 空库迁移、降级再升级和真实约束测试未执行 | 迁移可能存在隐含问题 | 在有 Docker 的环境中运行迁移验证 |
| TD-002 | M1 项目隔离和审计原子回滚的 PostgreSQL 集成测试未执行 | 隔离策略可能有边界遗漏 | 在有 Docker 的环境中运行集成测试 |
| TD-003 | M1 Docker Compose 启动和健康检查未实际执行 | 基础设施配置可能有问题 | 在有 Docker 的环境中验证 |
| TD-004 | M1 GitHub Actions 工作流尚未推送触发 | CI 配置可能有语法或环境问题 | 推送后验证 CI 运行结果 |
| TD-005 | M1 登录速率限制未实现 | 暴力破解风险 | M2 或安全加固阶段补充 |
| TD-006 | FastAPI TestClient 产生 1 条 Starlette/httpx 弃用警告 | 无功能影响，后续可能升级 | 后续依赖维护时处理 |

---

## 10. 关键决策记录

| 日期 | 决策 |
|---|---|
| 2026-06-25 | 产品采用 Web 应用，不开发桌面端 |
| 2026-06-25 | 数据采用项目内共享、项目间隔离 |
| 2026-06-25 | 只有超级管理员可以 CRUD 用户 |
| 2026-06-25 | 前端采用 Linear / Vercel 式高密度工作台 |
| 2026-06-25 | 控制面采用模块化单体，执行器采用独立 Worker |
| 2026-06-25 | 长任务统一使用 Temporal |
| 2026-06-25 | Playwright 负责回归，Browser Harness 负责探索 |
| 2026-06-25 | 开源框架接入时使用最新稳定版并锁定版本 |

---

## 11. 下一步建议

1. 编写 M2 详细实施计划（Agent、Dataset、TestCase、TestPlan 领域模型、迁移、API 和 UI）。
2. 在有 Docker 的环境中补验 M1：PostgreSQL 迁移、项目隔离、审计回滚和 Compose 启动。
3. 推送触发 GitHub Actions 并验证 CI 全部任务通过。
4. M1 环境验证通过后标记 M1 为已完成。
