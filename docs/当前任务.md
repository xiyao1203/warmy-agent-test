# 当前任务

本文件只保存一个当前活动任务，供 Codex 在新会话开始时快速恢复上下文。

---

## TASK-20260629-003：阶段十一 — 测试 Agent 对话入口（PRD 6.6）

- 状态：进行中
- 开始日期：2026-06-29
- 负责人：AI
- 关联需求：PRD 第 6.6 章
- 目标：左侧导航增加对话入口，自然语言驱动测试

## 本次范围

### 后端
- [ ] ChatAgent 实体（project_id / messages / plan_draft / status）
- [ ] 对话 API（POST /test-agent/chat — 发送指令返回计划草稿）
- [ ] 确认执行 API（POST /test-agent/confirm — 确认并触发执行）

### 前端
- [ ] 对话界面（消息列表 + 输入框 + 计划卡片）
- [ ] 计划卡片（可展开的结构化配置）
- [ ] 侧边栏新增"测试 Agent"导航

## 验收方式

- 后端：ruff ✅ / 179+ tests ✅
- 对话 API + 确认 API 完整可用
- 前端对话界面可用

## 下一步

- 全量评审 + 文档同步。
