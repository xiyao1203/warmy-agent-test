# 当前任务

本文件只保存一个当前活动任务，供 Codex 在新会话开始时快速恢复上下文。

完整历史记录见 `docs/开发进度与变更记录.md`。

---

## 当前状态

- 当前阶段：M2 测试资产
- 当前任务：TASK-20260625-006 M2 测试资产管理
- 当前子任务：详细计划 Task 6 测试资产 API 路由
- 状态：进行中
- 负责人：AI
- 开始日期：2026-06-25

## 任务目标

为 Agent、Dataset、TestPlan 和 EnvironmentTemplate 提供项目隔离、权限校验、CSRF 防护和稳定错误语义的 CRUD API。

## 执行依据

- MVP 路线图：`docs/superpowers/plans/2026-06-25-mvp-roadmap.md` 第 5 章
- PRD：Agent、数据集、用例和测试计划相关章节
- 技术架构：模块边界、不可变版本、项目隔离和数据不变量

## 前置完成状态

- M1 所有代码（Task 1–15）已完成并合并至 `main`。
- M2 详细计划、迁移、四个领域模型和应用层已完成并合并至 `main`。
- M2 已有代码的中文注释补充已完成。

## 本次允许修改

- `docs/superpowers/plans/`（M2 详细计划）
- `apps/control-api/`（M2 后端实现）
- `apps/web/`（M2 前端实现）
- `packages/generated-api-client/`（API Client 更新）
- `docs/api/`（API 文档）
- `docs/当前任务.md`
- `docs/开发进度与变更记录.md`

## 开发分支

- 分支：`codex/feature/m2-test-assets-api`
- 基线：`main` / `300f3d1`

## 当前进展

- 已创建开发分支 `codex/feature/m2-test-assets-api`。
- 已完成 M2 详细实施计划。
- 已完成数据库迁移（agents、datasets、test_plans、environments）。
- 已完成 Agents、Dataset、TestPlan、EnvironmentTemplate 四大领域模型和应用层。
- 新分支基线验证：133 个测试通过，4 个 PostgreSQL 测试跳过。
- Agents API 纵向切片已完成：Agent CRUD、版本 CRUD/发布、项目权限、CSRF 和启动依赖接入。
- Agents 契约与架构测试 8 个通过；Ruff 和 144 个源文件 mypy 通过。
- Dataset API 纵向切片已完成：Dataset/版本/用例 CRUD、导入导出、发布、项目路径隔离、CSRF 和启动依赖接入。
- Dataset 契约测试 6 个通过；Agents + Dataset + 架构测试 13 个通过；Ruff 和 146 个源文件 mypy 通过。
- 下一步：按同一契约模式实现 TestPlan API。
