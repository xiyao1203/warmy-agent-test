# 当前任务

本文件只保存一个当前活动任务，供 Codex 在新会话开始时快速恢复上下文。

---

## 当前活动任务

### TASK-20260712-001：对话式 Agent 全链路测试任务闭环

- 状态：进行中
- 开始日期：2026-07-12
- 负责人：AI
- 关联需求：PRD 3、5、6.1、6.6、7.1、8、9、13、15、17、19
- 关联架构：技术架构 1-4、6、8-10、12、14、15、18、21；设计规格 `2026-07-12-conversational-agent-test-mission-design.md`
- 目标：用户通过自然语言提供目标 URL 和测试目标后，平台能自动识别任务、最小化补齐必要数据、只读探测目标、一次确认后自动创建或复用平台资产并执行 API、浏览器、安全、评分、审核和门禁全链路测试。
- 允许修改：`apps/web`、`apps/control-api`、`workers`、`plugins`、`packages`、`infra`、数据库迁移、测试和相关文档。
- 禁止修改：无关产品功能；不得引入 Mock/假成功；不得把密码、Token、Cookie 或明文登录态写入对话、快照、日志或 Trace；不得让模型绕过权限、预算、动作 Allowlist 或确认。
- 验收方式：TDD；前端 format/lint/typecheck/unit/E2E/build；后端 lint/typecheck/unit/integration/API/architecture；数据库空库与上一版迁移、约束、索引和项目隔离；Workflow replay/retry/timeout/cancel/resume/idempotency；Fake Target 全链路；至少一个真实目标 Agent 只读全链路验收。
- 当前阶段：完整设计已逐段获得用户确认，正在固化书面规格；规格最终确认后编写实施计划并开始开发。
- 相关阻塞：上一阶段 TapNow 真实成功 Run 仍受专用账号登录态、Codex usage limit 和 Tapies 额度影响；不阻塞本轮通用功能开发，但会影响真实 TapNow 外部上线门禁。
