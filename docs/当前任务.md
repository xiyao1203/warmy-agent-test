# 当前任务

本文件只保存一个当前活动任务，供 Codex 在新会话开始时快速恢复上下文。

完整历史记录见 `docs/开发进度与变更记录.md`。

---

## TASK-20260626-001：M3 通用 HTTP Agent 测试闭环

- 状态：进行中
- 开始日期：2026-06-26
- 负责人：AI
- 当前阶段：M3 前端运行体验打磨与真实环境验收前收口
- 开发分支：`codex/feature/m3-ui-polish`（方案 1 UI 打磨分支，承接 M3）
- 关联需求：PRD 第 7.3、8.6、8.8、8.9、15、17.1、19.3 章
- 关联架构：技术架构第 4.2、4.3、8.4、8.5、9.2、9.3、12、13、15.4 章及 ADR-0002

## 目标

交付基于已发布测试计划的 Run/RunCase 创建、执行、进度、取消、结果与 Trace 前后端闭环，并按方案 1 管理台风格打磨前端 UI 与交互。

## 本次范围

- `apps/web/src/components/layout/`：管理台应用壳、顶部搜索区、用户区和项目导航命名打磨
- `apps/web/src/features/runs/`：运行中心统计卡、搜索/状态筛选、空状态、运行详情摘要侧栏与实时状态提示
- `apps/web/src/features/projects/tests/`：同步项目概览现有文案断言
- `docs/`：同步当前任务、开发进度和 M3 实施计划

## 验收方式

- 前端：运行中心/详情组件测试、Web lint、TypeScript 类型检查、Next.js build
- 文档：当前任务和开发进度记录更新，未完成的真实环境验证继续保留

## 当前进展

- 已创建新开发分支 `codex/feature/m3-ui-polish`。
- 已确认本轮前端继续按照此前约定的“方案 1”管理台布局和交互方向推进，并保留 Agent 流式对话页面的三栏工作区设计。
- 已完成应用壳顶部搜索/帮助/通知/用户区、项目导航命名与运行中心页面层级打磨。
- 已完成运行中心总览统计、搜索、状态筛选、重置、匹配空状态和表格 hover 细节。
- 已完成运行详情“执行摘要”侧栏、实时刷新状态、进度条、用例统计和运行元信息展示。
- 已修复资产列表操作列挤压问题：Agent、数据集、测试计划列表统一使用不换行的横向表格操作区，避免“查看/删除”被拆成竖排。
- 已优化资产列表表头与字段布局：Agent、数据集、测试计划列表改为固定列宽、中文语义表头、浅色表头背景、行 hover 和名称/描述截断，避免宽屏下字段漂移和大面积空白。
- 已微调资产列表首列表头对齐：表头文字与行内名称文字起点对齐，避开图标列造成的视觉错位。
- 已重新收紧资产列表列组：表格改为内容宽度，短字段列定宽居中，操作列居中，避免类型/时间/操作字段被拉散到页面中间。
- 已补前端运行详情 SSE 退避重连：连接异常后按 1s、2s、4s 最多 3 次显式重连，超过次数后回退查询轮询。
- 已同步相关组件测试。

## 本轮验证记录（2026-06-26）

- `pnpm --filter @warmy/web test -- src/features/runs/tests/run-center.test.tsx`：13 个测试文件、43 个测试通过。
- `pnpm --filter @warmy/web lint`：通过。
- `pnpm --filter @warmy/web typecheck`：通过。
- `pnpm --filter @warmy/web build`：通过。
- 修复资产列表操作列后复验 `pnpm --filter @warmy/web test -- src/features/agents/tests/agent-list.test.tsx src/features/datasets/tests/dataset-list.test.tsx src/features/test-plans/tests/test-plan-list.test.tsx`：13 个测试文件、43 个测试通过。
- 修复资产列表操作列后复验 `pnpm --filter @warmy/web lint`：通过。
- 修复资产列表操作列后复验 `pnpm --filter @warmy/web typecheck`：通过。
- 修复资产列表操作列后复验 `pnpm --filter @warmy/web build`：通过。
- 优化资产列表表头与字段布局后复验 `pnpm --filter @warmy/web test -- src/features/agents/tests/agent-list.test.tsx src/features/datasets/tests/dataset-list.test.tsx src/features/test-plans/tests/test-plan-list.test.tsx`：13 个测试文件、43 个测试通过。
- 优化资产列表表头与字段布局后复验 `pnpm --filter @warmy/web lint`：通过。
- 优化资产列表表头与字段布局后复验 `pnpm --filter @warmy/web typecheck`：通过。
- 优化资产列表表头与字段布局后复验 `pnpm --filter @warmy/web build`：通过。
- 微调资产列表首列表头对齐后复验 `pnpm --filter @warmy/web test -- src/features/agents/tests/agent-list.test.tsx src/features/datasets/tests/dataset-list.test.tsx src/features/test-plans/tests/test-plan-list.test.tsx`：13 个测试文件、43 个测试通过。
- 微调资产列表首列表头对齐后复验 `pnpm --filter @warmy/web lint`：通过。
- 微调资产列表首列表头对齐后复验 `pnpm --filter @warmy/web typecheck`：通过。
- 收紧资产列表列组后复验 `pnpm --filter @warmy/web test -- src/features/agents/tests/agent-list.test.tsx src/features/datasets/tests/dataset-list.test.tsx src/features/test-plans/tests/test-plan-list.test.tsx`：13 个测试文件、43 个测试通过。
- 收紧资产列表列组后复验 `pnpm --filter @warmy/web lint`：通过。
- 收紧资产列表列组后复验 `pnpm --filter @warmy/web typecheck`：通过。
- 补 SSE 退避重连后复验 `pnpm --filter @warmy/web test -- src/features/runs/tests/run-center.test.tsx`：13 个测试文件、44 个测试通过。
- 补 SSE 退避重连后复验 `pnpm --filter @warmy/web lint`：通过。
- 补 SSE 退避重连后复验 `pnpm --filter @warmy/web typecheck`：通过。
- 补 SSE 退避重连后复验 `pnpm --filter @warmy/web build`：通过。

## 未验证/遗留风险

- 未验证真实 PostgreSQL 空库迁移、上一版本升级、约束和索引。
- 未验证 Docker Compose 与真实 Temporal Server 执行。
- 未验证真实 Temporal Server 启动、Workflow replay history、真实取消清理、Artifact 上传。
- 未完成真实 Artifact 上传与下载。
- 前端 SSE 已有退避重连和断线查询刷新兜底。

## 本轮补充（2026-06-27）

- 已创建分支 `codex/feature/m3-artifacts`。
- 已完成 Artifact 产物后端全链路（迁移/ORM/API）。
- 已完成前端运行详情产物区（列表+下载按钮）。
- 已验证：ruff/mypy/TypeCheck/Lint ✅ / 111 tests ✅
- 已完成前端产物上传入口：文件选择 + 上传按钮 + loading + 错误提示
- 已同步 CSRF 头修复

## 下一步

- M3 Artifact 产物闭环已完成（后端 API + 前端展示/上传），待合成环境验证。
- 在真实 Temporal Server 环境验证 `RunOrchestrator` Adapter 和 Workflow 内部结果回写闭环。
- 在可用 Docker/PostgreSQL/Temporal 环境补齐 M1/M3 环境验证。
