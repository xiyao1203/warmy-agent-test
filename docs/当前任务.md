# 当前任务

本文件只保存一个当前活动任务，供 Codex 在新会话开始时快速恢复上下文。

---

## 当前活动任务

### TASK-20260712-002：Agent 测试可信闭环系统优化

- 状态：待验证
- 开始日期：2026-07-12
- 负责人：AI
- 关联需求：PRD 3、5、6.1、6.6、7-9、13、15、17、19
- 关联架构：技术架构 1-4、6、8-10、12-15、17、18、21；设计规格 `docs/superpowers/specs/2026-07-12-agent-testing-trust-loop-optimization-design.md`
- 目标：实现执行可信、评估可信、诊断可证、回归可复现、门禁可解释的 Agent 测试闭环，并吸收 `TASK-20260712-001` 尚缺的真实 Fake Target 公共路径验收。
- 允许修改：`apps/web`、`apps/control-api`、`workers`、`plugins`、`packages`、`infra`、数据库迁移、测试和相关文档。
- 禁止修改：无关产品功能；不得引入 Mock/假成功；不得把密码、Token、Cookie 或明文登录态写入对话、快照、日志、Trace 或 Temporal History；不得让模型修改原始 Evidence、绕过权限、预算、动作 Allowlist、确认或直接决定发布。
- 实施计划：`docs/superpowers/plans/2026-07-12-agent-testing-trust-loop-optimization.md`
- 验收方式：TDD；生产同构 Fake Target 公共路径；Outcome/Evidence、故障分类、诊断、最小复现、Quarantine、基线与联合门禁测试；前端 format/lint/typecheck/unit/E2E/build；后端 lint/typecheck/unit/integration/API/architecture；数据库空库与上一版迁移、约束、索引和项目隔离；Workflow replay/retry/timeout/cancel/resume/idempotency；至少一个真实目标 Agent 只读全链路验收。
- 当前阶段：可信闭环代码、生产同构 Fake Target 和公共路径验收已完成；仅剩需要外部专用账号/登录态/额度的真实目标只读验收。
- 已验证：`make verify` 除首次生成 OpenAPI 差异外的全部门禁通过，生成产物已提交；Python 全仓 881 passed / 7 skipped，隔离 PostgreSQL 19 passed，公共路径 Mission `5ad59a30-1375-4d44-b2d7-a59e5f47502f` completed、Run `17abb560-488c-4ad4-8be7-866bede56e00` passed（3/3）。
- 待验证：使用用户批准的真实外部目标和专用测试身份执行一次只读 Mission；当前仓库没有可用的目标账号、登录态和额度，不能用 Fake Target 结果冒充真实目标证据。
