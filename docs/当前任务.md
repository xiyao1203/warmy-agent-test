# 当前任务

本文件只保存一个当前活动任务，供 Codex 在新会话开始时快速恢复上下文。

---

## 当前活动任务

### TASK-20260711-002：TapNow 类 Agent 生产测试闭环实现与联调

- 状态：进行中
- 开始日期：2026-07-11
- 负责人：AI
- 关联需求：PRD 3、5、6.1、7.1、8.7、9、13、17、19
- 关联架构：技术架构 1-4、8-10、12、14、15、18、21；设计规格 `2026-07-11-tapnow-production-test-loop-design.md`、`2026-07-11-browser-profile-auth-state-production-design.md`
- 目标：在现有平台内一次性完成 TapNow 类 Agent 的前端、后端、数据库、Worker、插件、评测、安全和结果闭环，并通过真实联调测试。
- 允许修改：`apps/web`、`apps/control-api`、`workers`、`plugins`、`packages/generated-api-client`、`infra`、相关测试与文档。
- 禁止修改：无关产品功能；不得引入 Mock/假成功；不得把明文凭证写入快照、日志或 Trace。
- 验收方式：前后端与 Worker 全量相关测试、迁移测试、Temporal/错误/取消/幂等测试、前端 lint/typecheck/test/build、真实浏览器目标联调、`git diff --check` 全部通过。
- 当前目标：参考 `xiyao1203/runtest@ac0bfd4d`，完成数据库化浏览器实例、加密登录态快照、短期运行租约、TapNow 复用、证据采集和前端状态闭环；保留现有平台权限、项目隔离和 Worker 边界。
- 已完成：TapNow 基础生产闭环、迁移 `0018`、短期凭证租约、Artifact、DeepEval、Promptfoo、审核/门禁证据和运行详情已实现；浏览器登录态生产规格已确认并完成参考项目差异分析。
- 当前开发范围：迁移 `0019`、浏览器实例 Domain/Application/Infrastructure/API、Worker BrowserContext 与 Artifact、TapNow 插件状态、Agent 发布校验、前端浏览器实例/Agent/Run 详情、测试和文档。
- 外部上线门禁：代码与 Fake Target 联调完成后，仍须使用 TapNow 专用账号和有效额度完成一个真实只读成功 Run；若账号、Codex 或 Tapies 额度不可用，任务只能标记阻塞，不能宣称上线完成。
