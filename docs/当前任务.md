# 当前任务

本文件只保存一个当前活动任务，供 Codex 在新会话开始时快速恢复上下文。

---

## 当前活动任务

### TASK-20260712-003：Run 可信闭环能力产品化

- 状态：进行中
- 开始日期：2026-07-12
- 负责人：AI
- 关联需求：PRD 3、5、6.1、6.6、7-9、13、15、17、19
- 关联架构：技术架构 1-4、6、8-10、12-15、17、18、21；设计规格 `docs/superpowers/specs/2026-07-12-run-trust-loop-productization-design.md`
- 目标：让普通 Run 与 Mission Run 在终态后自动完成确定性分类、证据约束诊断、两次复现回归、评估校准和不可补偿联合门禁，并通过项目隔离 API 与前端展示真实持久化结果。
- 允许修改：`apps/control-api`、`workers/api-runner`、`apps/web`、`packages/generated-api-client`、`tests/fake_agent_target`、`tests/acceptance`、数据库迁移、OpenAPI 和相关文档。
- 禁止修改：无关产品功能；不得让 Worker 连接业务数据库；不得把秘密写入 Workflow History、Evidence、日志或诊断；不得让模型修改事实、发布回归或决定门禁。
- 实施计划：`docs/superpowers/plans/2026-07-12-run-trust-loop-productization.md`
- 验收方式：TDD；后处理状态机、幂等任务、项目隔离迁移与 API；Temporal replay/retry/cancel/recovery；Fake Target 公共路径故障矩阵；前端 format/lint/typecheck/unit/E2E/build；全仓 `make verify` 和隔离 PostgreSQL 验证。
- 当前阶段：已完成后处理编排、公开 API、Mission 共享投影和 Run/测试 Agent 统一展示，正在验证 Fake Target 公共路径故障矩阵与 Workflow 恢复能力。
- 独立待验证：`TASK-20260712-002` 的真实外部目标只读验收仍需要用户批准的专用账号、登录态和额度，不属于本任务本地闭环完成条件。
