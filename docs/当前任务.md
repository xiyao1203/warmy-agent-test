# 当前任务

本文件只保存一个当前活动任务，供 Codex 在新会话开始时快速恢复上下文。

---

## 当前活动任务

### TASK-20260703-001：Agent 对话 Codex 式全链路体验优化

- 状态：待验证
- 开始日期：2026-07-03
- 负责人：AI
- 分支：`codex/fix/agent-chat-ui-feedback`
- 关联需求：PRD 4.1、5.1、6.1、6.6、8.8、15.2
- 关联架构：技术架构 2.4、4.2、6、7、8、14；现有 Temporal Model Runner 与 Test Agent 模块
- 目标：实现可恢复、可取消、无首包竞态的 Agent 对话链路，并采用 ChatGPT 正文风格与 Codex 内联过程时间线。
- 计划修改：Test Agent 会话/事件/生成状态 API、Temporal 取消链路、Web 对话状态机与时间线组件、迁移、测试和文档。
- 验收方式：前后端单元与契约测试、数据库迁移与项目隔离测试、SSE 重连/恢复/取消测试、浏览器关键交互检查、全量质量门禁。
- 设计规格：`docs/superpowers/specs/2026-07-03-agent-chat-codex-experience-design.md`、`docs/superpowers/specs/2026-07-03-agent-chat-balanced-ui-refinement-design.md`
- 已完成：生成状态持久化、语义事件恢复、SSE ready/游标补发、Temporal 真实取消、确认后同轮恢复、Codex 内联时间线与 ChatGPT 式正文/输入区；修复未登录加载死锁、渲染期跳转、generation 事件漏订阅、停止后部分正文丢失和自动重连重复回放；完成克制增强版 UI、工具状态归并、确认折叠、悬浮输入区、上下文/历史抽屉和窄屏默认收起；根据截图反馈修复删除遮挡、双层焦点、原始推理暴露和强制追问胶囊。
- 已验证：定向后端/Worker 63 passed；Test Agent 后端 39 passed、1 PostgreSQL-only skipped；本轮 SSE 重连契约 2 passed；Web Agent/Reasoning 17 passed、窄屏壳层 1 passed；迁移空 SQLite 升级至 `0015`；Lint 无错误、格式、变更域 mypy、TypeScript、生成客户端、OpenAPI drift、Web build 通过；浏览器 1440/1024/390px 无横向溢出。
- 待验证：真实 Temporal + Model Runner 取消/部分内容 E2E。
- 既有门禁：全仓格式 34 个未改文件失败；全仓 Web 6 个账户/主题测试失败；全仓后端 597 passed、6 skipped、2 个既有架构失败；全量 mypy 仍有 PydanticAI 适配既有错误。

### 最近完成

- TASK-20260702-004：浏览器实例注册表前后端完整实现（完成于 2026-07-02）
  - 后端：新增 browser_profiles 模块，FastAPI CRUD API（list/create/get/update/delete）
  - 前端：新增 BrowserProfileList 管理页面（列表/创建/编辑/删除）
  - 测试计划集成：TestPlanConfig 新增 browser_profile_id 字段，版本对话框新增实例选择
  - Worker 层：workflow 新增 plan config fallback 支持
  - OpenAPI 导出 + TypeScript Client 重新生成

- TASK-20260702-003：Codex 浏览器生命周期管理与登录态复用（完成于 2026-07-02）
  - 新增 BrowserMode（ephemeral/persistent）、StorageStateConfig
  - 持久 Chrome 进程管理（ensure/health-check/shutdown）
  - storageState 登录态读写（load/save/TTL 过期）
  - Worker 层 browser_mode/storage_state_key/credentials 字段透传

下一步建议：登录本地工作台完成对话视觉验收，并在真实 Temporal/Model Runner 环境验证取消链路后关闭当前任务。
