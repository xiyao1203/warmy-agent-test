# 当前任务

本文件只保存一个当前活动任务，供 Codex 在新会话开始时快速恢复上下文。

---

## 当前活动任务

### TASK-20260711-002：TapNow 类 Agent 生产测试闭环实现与联调

- 状态：阻塞
- 开始日期：2026-07-11
- 负责人：AI
- 关联需求：PRD 3、5、6.1、7.1、8.7、9、13、17、19
- 关联架构：技术架构 1-4、8-10、12、14、15、18、21；设计规格 `2026-07-11-tapnow-production-test-loop-design.md`
- 目标：在现有平台内一次性完成 TapNow 类 Agent 的前端、后端、数据库、Worker、插件、评测、安全和结果闭环，并通过真实联调测试。
- 允许修改：`apps/web`、`apps/control-api`、`workers`、`plugins`、`packages/generated-api-client`、`infra`、相关测试与文档。
- 禁止修改：无关产品功能；不得引入 Mock/假成功；不得把明文凭证写入快照、日志或 Trace。
- 验收方式：前后端与 Worker 全量相关测试、迁移测试、Temporal/错误/取消/幂等测试、前端 lint/typecheck/test/build、真实浏览器目标联调、`git diff --check` 全部通过。
- 已完成：前端、控制面、迁移 `0018`、Worker、TapNow 插件、短期凭证租约、Artifact、DeepEval、Promptfoo、人工审核/门禁证据和运行详情闭环均已实现；本地全栈错误链路已完成真实 API/Temporal/Worker/数据库回写。
- 阻塞条件：项目 `05e980c3-50fe-4bd9-9eed-a44b5842e70c` 当前没有 TapNow `username/password` 凭证绑定，应用内浏览器被重定向到 TapNow 登录页；Codex 探索账号同时达到 usage limit。缺少这两个外部条件时不能形成真实目标成功 Run。
- 恢复条件：在平台项目中保存并绑定专用 TapNow 测试账号凭证，恢复 Codex 用量，并确认 TapNow 账号有可用 Tapies/调用额度；随后按 `docs/runbooks/tapnow-production-testing.md` 重跑真实 Run 并记录 Artifact、评分、Finding、Review 和 Gate 证据。
