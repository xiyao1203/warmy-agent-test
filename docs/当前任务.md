# 当前任务

本文件只保存一个当前活动任务，供 Codex 在新会话开始时快速恢复上下文。

完整历史记录见 `docs/开发进度与变更记录.md`。

---

## 当前状态

- 当前阶段：M1 平台基础
- 当前任务：TASK-20260625-003 实现登录、用户与项目权限
- 状态：进行中
- 负责人：AI
- 开始日期：2026-06-25

## 任务目标

实现超级管理员用户 CRUD、服务端可撤销 Session、项目成员权限和后端项目数据隔离。

## 执行依据

- 关联需求：PRD 第 8.1、16、19.1 章
- 关联架构：技术架构规范第 7.7、7.8、8.3、8.4 章
- 详细计划：`docs/superpowers/plans/2026-06-25-m1-platform-foundation.md` Task 8–15

## 本次允许修改

- `apps/control-api/`
- `apps/admin-cli/`
- `apps/web/`
- `packages/generated-api-client/`
- `docs/api/`
- `docs/adr/`
- `scripts/`
- `.github/workflows/`
- 根工程配置、Lockfile、README
- 当前任务和开发进度文档

## 本次禁止修改

- PRD 产品范围
- 已确认的核心架构决策
- M2 及后续业务模块
- Worker 和插件实现

## 验收条件

- 登录、退出、当前用户和服务端 Session 安全测试通过。
- 仅超级管理员可以管理用户，且不能禁用或删除最后一个可用超级管理员。
- 普通用户只能访问被分配项目，跨项目直接访问返回 404。
- 用户禁用、密码重置或移出项目后权限立即失效。
- 身份、项目和审计数据库迁移及约束验证通过。
- 前端登录、用户管理、项目切换和权限状态通过组件与 E2E 验证。
- OpenAPI Client 无漂移，代码、迁移、API 和文档保持一致。

## 开发分支

- 分支：`codex/feature/m1-openapi-foundation`
- Worktree：当前仓库工作区

## 当前进展

- 已恢复仓库上下文并审阅 PRD、架构规范和 M1 详细计划。
- 已创建开发分支。
- 详细计划 Task 8 已完成：身份领域值对象、用户实体、仓储端口、共享时钟/ID 和惰性数据库会话工厂。
- 已补齐 Task 6 的架构边界检查器与 4 个规则测试。
- 详细计划 Task 9 代码已完成：Alembic 基线、身份/Session/项目/审计表、约束、索引和 ORM 模型。
- Task 9 的离线 PostgreSQL SQL 与模型约束已验证；真实数据库升级循环和唯一约束测试因环境缺少 PostgreSQL而跳过。
- 详细计划 Task 10 已完成：Argon2id 登录、随机服务端 Session、Token Hash、当前用户、退出撤销、Cookie 和 CSRF 防护。
- 详细计划 Task 11 已完成：超级管理员初始化 CLI、用户分页查询与 CRUD API、最后管理员和自我降权保护、密码重置撤销 Session、历史用户软删除。
- Task 12 领域与 Policy 已完成：项目创建/重命名/归档、成员增删改、超级管理员全局访问、普通成员隔离和未授权 404 语义。
- 已新增 `identity/public.py`，项目模块仅通过公开出口依赖身份类型。
- Task 12 实现已完成：项目应用用例、SQLAlchemy 仓储、项目与成员 API、普通成员列表隔离、跨项目 404 和移除即时失权。
- Task 12 真实 PostgreSQL 隔离测试已编写，当前环境缺少数据库而跳过。
- Task 13 审计基础已完成：稳定公开端口、结构化 AuditEntry、递归敏感字段脱敏和 actor/object/project/source IP 记录。
- Task 13 审计持久化与 API 已完成：全局审计仅超级管理员、项目审计按成员权限、跨项目返回 404。
- 登录成功/失败、退出、用户创建/编辑/重置/启停/删除、项目创建/重命名/归档和成员变化已接入审计端口。
- Task 13 Unit of Work 已完成：HTTP 写请求中的身份、项目和审计仓储复用同一 AsyncSession，由最外层统一提交或回滚。
- 真实 PostgreSQL 原子回滚测试已编写，当前环境缺少数据库而跳过。
- `codex/feature/m1-auth-project-permissions` 已合并至 `main`，合并提交为 `6740a78`。
- 已创建新分支 `codex/feature/m1-openapi-foundation`，基线验证通过。
- 详细计划 Task 5 已完成：稳定 OpenAPI 导出、`@hey-api/openapi-ts 0.99.0`、bundle Fetch Client、类型模型/SDK 和漂移检查。
- 详细计划 Task 7 已完成：ADR-0001 至 ADR-0008 已按统一模板记录并与架构文档一致。
- 详细计划 Task 6 已完成：GitHub Actions 覆盖 Python、PostgreSQL 集成、Web、契约、Compose 和安全检查，Dependabot 覆盖 npm、uv、Actions 和 Docker。
- CI 使用完整 Commit SHA 固定第三方 Action；本地全量 `make verify`、Python/Node 安全审计和 YAML 校验通过。
- Task 14 视觉方向已确认采用“方案 1：极简控制台”，并保留未来 Agent 三栏流式工作区。
- Task 14 设计说明已形成，待审阅后编写实施计划并按 TDD 开发。
- 下一步：审阅 M1 平台界面设计说明。

## 验证记录

- `uv run pytest apps/control-api/tests apps/admin-cli/tests -q`：66 个测试通过，4 个 PostgreSQL 测试跳过。
- `uv run ruff check apps/control-api apps/admin-cli`：通过。
- `uv run mypy apps/control-api/src apps/admin-cli/src`：通过（82 个源文件）。
- `uv run pytest apps/control-api/tests/architecture -v`：4 个测试通过。
- `uv run python scripts/check_architecture.py`：通过。
- `uv run agenttest-admin --help`：通过。
- `make api-generate`：通过。
- `pnpm --filter @warmy/generated-api-client test`：1 个测试通过。
- `pnpm --filter @warmy/generated-api-client typecheck`：通过。
- `make api-check`：通过。
- `make verify`：通过；包含格式、Lint、类型检查、66 个后端/CLI 测试、2 个前端/Client 测试、Web 构建、架构检查和 OpenAPI 漂移检查。
- CI/Dependabot YAML 解析和 21 个 Action 完整 Commit SHA 固定检查：通过。
- `uv run --with pip-audit==2.10.1 pip-audit`：无已知第三方 Python 依赖漏洞；两个本地包不在 PyPI，按预期跳过。
- `pnpm audit --audit-level high`：通过；存在 1 个中等级依赖问题，未达到高危门禁阈值。
- PostgreSQL 空库升级、降级再升级、真实唯一约束、项目隔离和审计原子回滚：未验证；当前环境没有 Docker、psql 或可用数据库。
- Compose 启动和 GitHub 托管 CI 实际运行：未验证；当前环境没有 Docker，工作流尚未推送触发。
- FastAPI TestClient 产生 1 条上游 Starlette/httpx 弃用警告；不影响测试结果，后续依赖维护处理。

## 已知前置技术债

- 前端尚无已确认 UI 原型；Task 14 开始前需完成设计上下文确认。
- M1 最终验收前需在 Docker/GitHub 环境执行 PostgreSQL、Compose 和托管 CI 验证。
