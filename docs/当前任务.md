# 当前任务

本文件只保存一个当前活动任务，供 Codex 在新会话开始时快速恢复上下文。

---

## TASK-20260628-002：阶段二 — 测试数据集与用例增强（PRD 8.3）

- 状态：已完成
- 完成日期：2026-06-28
- 分支：codex/feature/phase2-datasets-enhancement
- 目标：用例支持高级字段、多格式导入导出、版本分组

### 实现摘要

**后端（已完成）：**
- TestCase 领域模型已包含所有 PRD 8.3 字段（initial_state、expected_outcome、assertions、scorers、security_policies、tags、priority、risk_level、difficulty、test_group）
- ORM 模型已完整映射
- 从失败运行生成用例 API：`POST /{dataset_id}/versions/{version_id}/generate-from-run`
- CSV 导入增强：字段映射、校验、结果报告

**前端新增/增强：**
- `test-case-editor.tsx`：增强用例编辑器，支持分区表单（基本信息/输入数据/预期输出/评分与安全/高级选项）
- `test-case-detail.tsx`：用例详情抽屉，完整展示所有字段
- `dataset-detail.tsx`：集成详情抽屉，用例列表增加查看详情按钮

### 验证记录

- 后端测试：295 passed / 3 failed（已有问题）/ 4 skipped

## 已完成任务汇总

| 阶段 | PRD 章节 | 状态 |
|------|----------|------|
| 一 | 8.2 Agent版本管理 | ✅ 已完成 |
| 二 | 8.3 测试数据集与用例 | ✅ 已完成 |

## 下一步

按计划进入阶段三：测试环境管理（PRD 8.4）
