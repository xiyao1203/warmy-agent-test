export function projectWorkspacePath(projectId: string) {
  return `/projects/${projectId}/test-agent`;
}

export function projectOverviewPath(projectId: string) {
  return `/projects/${projectId}/overview`;
}
