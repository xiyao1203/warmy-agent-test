# AgentTest AI 开发协作规范

本文件是本仓库所有 AI Agent 和开发人员必须遵守的仓库级开发协议。

目标是让不同会话、不同 Agent 和不同开发人员始终基于同一产品目标、架构约束和当前进度工作，避免重复开发、遗漏验证、架构漂移和上下文丢失。

---

## 0. Codex 快速启动

Codex 进入仓库后必须先执行以下流程：

1. 阅读本文件。
2. 阅读 `docs/当前任务.md`，确认唯一活动任务、允许修改范围和验收条件。
3. 阅读 `docs/开发进度与变更记录.md` 的项目状态、当前进行中、阻塞和已知问题。
4. 根据当前任务按需阅读 PRD、技术架构、ADR、计划和模块文档。
5. 检查 `git status`、最近提交和已有实现。
6. 用不超过 8 行复述：当前目标、范围、约束、计划和验证方式。
7. 只有在任务已登记且上下文无冲突时才开始修改。

当 `docs/当前任务.md` 显示“当前无活动任务”时：

- 用户给出明确任务：先登记为当前任务，再开发。
- 用户只说“继续开发”：从进度台账选择第一个前置条件已满足的待开始任务，并先向用户说明选择。
- 存在多个同等优先任务：不得自行并行展开，先请求用户确认。

Codex 专用操作和提示词见 `docs/Codex开发指南.md`。

---

## 1. 开始任何开发前必须阅读

按以下顺序阅读：

1. `AGENTS.md`
2. `docs/当前任务.md`
3. `docs/开发进度与变更记录.md`
4. `docs/Agent测试平台产品需求文档-PRD.md`
5. `docs/Agent测试平台技术架构与开发规范.md`
6. 当前实施计划、ADR、模块 README、测试和局部 `AGENTS.md`

如果任务涉及特定模块，只需重点阅读相关章节，但不得跳过：

- PRD 的产品定位、权限、数据隔离和验收标准。
- 架构文档的依赖边界、目录规范、禁止事项和架构验收标准。
- 开发进度文档中的当前阶段、已完成工作、进行中任务和已知问题。

在没有完成上述阅读前，不得开始写代码、创建目录、引入依赖或修改数据库。

---

## 2. 文档优先级

发生冲突时按以下优先级处理：

1. 用户当前明确指令。
2. 本文件 `AGENTS.md`。
3. 产品需求文档 PRD。
4. 技术架构与开发规范。
5. 已批准的 ADR。
6. 当前实施计划。
7. 现有代码惯例。

如果 PRD 与技术架构冲突，不得自行选择。先在开发记录中标记冲突，并请求确认或创建 ADR。

---

## 3. 开发前工作协议

### 3.1 检查当前状态

开发前必须：

- 查看 Git 状态，保留用户已有修改。
- 阅读最新开发进度。
- 搜索是否已有相同功能、接口、组件、迁移或测试。
- 确认任务属于哪个产品模块。
- 确认是否存在前置任务或阻塞问题。
- 确认当前开发是否符合 MVP 范围。

### 3.2 创建任务记录

开始开发前必须同时更新：

- `docs/当前任务.md`：只保存当前唯一活动任务。
- `docs/开发进度与变更记录.md`：“进行中”部分保存任务登记。

任务格式：

```markdown
### TASK-YYYYMMDD-序号：任务名称

- 状态：进行中
- 开始日期：YYYY-MM-DD
- 负责人：AI / 姓名
- 关联需求：PRD 章节
- 关联架构：架构文档章节或 ADR
- 目标：一句话描述可验证结果
- 计划修改：文件或模块列表
- 验收方式：测试、命令或人工检查
```

同一时间不得创建名称和目标重复的进行中任务。

除经用户明确同意的并行子任务外，仓库只保留一个主活动任务。

### 3.3 复杂任务必须先计划

符合以下任一条件时，必须先编写实施计划：

- 涉及两个及以上业务模块。
- 涉及数据库迁移。
- 涉及认证、权限、凭证或安全。
- 涉及 Workflow、Worker 或插件协议。
- 引入或升级核心开源框架。
- 预计修改超过 5 个主要文件。
- 需求存在多种合理实现。

计划保存在：

```text
docs/superpowers/plans/YYYY-MM-DD-任务名称.md
```

---

## 4. 开发过程规范

### 4.1 遵守架构边界

必须遵守：

- 前端按 Feature 组织，通过公开出口依赖。
- 后端使用 Domain、Application、Infrastructure、API 分层。
- 模块之间只使用公开接口或事件。
- API 不直接操作 ORM。
- Worker 不直接连接业务数据库。
- 插件只依赖公开 Plugin SDK。
- 所有项目业务数据强制关联并校验 `project_id`。
- 已发布的 Agent、数据集、用例和测试计划使用不可变版本。

详细规则以 `docs/Agent测试平台技术架构与开发规范.md` 为准。

### 4.2 测试驱动和小步提交

- 新功能和缺陷修复先补充失败测试，再实现。
- 每次修改保持单一目标。
- 不顺手重构无关代码。
- 不删除或覆盖用户已有修改。
- 不使用跳过测试、忽略类型或关闭规则掩盖问题。
- 每完成一个独立任务，运行与风险匹配的验证。

### 4.3 依赖管理

- 引入开源框架前核对官方最新稳定版。
- 禁止使用浮动 `latest`、Git `main` 或未固定 Commit。
- 必须提交 Lockfile。
- 记录许可证、安全、兼容性和回滚方案。
- 升级依赖时不得混入无关功能开发。

### 4.4 数据库修改

- 所有 Schema 修改通过 Alembic。
- 迁移文件创建后不得改写已发布历史。
- 使用 Expand → Migrate → Contract。
- 新表和核心查询必须说明项目隔离、外键、唯一约束和索引。
- 数据迁移必须提供验证和回滚/补偿方案。

### 4.5 安全

- 禁止提交密码、Token、Cookie、API Key、测试账号或真实用户数据。
- 日志、Trace 和测试产物必须脱敏。
- 前端隐藏入口不能代替后端权限校验。
- 外部输入、模型输出、Markdown、HTML、URL 和文件均视为不可信。
- 高风险操作必须有确认、权限检查和审计。

---

## 5. 完成开发前必须验证

不得在没有证据的情况下声称“完成”“修复”或“通过”。

根据改动范围运行：

### 前端

```text
format
lint
typecheck
unit/component tests
critical Playwright E2E
build
```

### 后端

```text
format
ruff/lint
typecheck
unit tests
integration tests
API contract tests
architecture tests
```

### 数据库

```text
空数据库迁移
上一版本数据库升级
约束和索引验证
项目数据隔离测试
```

### Worker / Workflow

```text
Workflow replay
timeout/retry/cancel
idempotency
artifact upload
error classification
```

如果环境原因无法运行某项验证，必须在开发记录中明确写出“未验证”、原因和风险，不能用推测代替。

---

## 6. 每次开发完成后必须更新记录

完成任务后，必须更新 `docs/开发进度与变更记录.md`：

1. 将任务从“进行中”移动到“已完成”。
2. 记录实际修改的文件和模块。
3. 记录数据库、API、事件和配置变更。
4. 记录执行过的测试及结果。
5. 记录未完成内容、已知问题和技术债。
6. 记录下一步建议。
7. 更新当前里程碑完成度。

同时更新 `docs/当前任务.md`：

- 当前任务已完成且尚未选择下一任务：设为“当前无活动任务”。
- 用户已明确要求连续执行下一任务：登记下一任务及允许修改范围。
- 当前任务阻塞：记录阻塞条件和恢复开发所需输入。

完成记录格式：

```markdown
### TASK-YYYYMMDD-序号：任务名称

- 状态：已完成
- 完成日期：YYYY-MM-DD
- 关联需求：PRD 章节
- 实现摘要：
  - ...
- 变更文件：
  - `path/to/file`
- 数据库/API/配置变化：
  - 无 / 具体内容
- 验证记录：
  - `具体命令`：通过
- 遗留问题：
  - 无 / 具体内容
- 下一步：
  - ...
```

代码和记录属于同一个任务。没有更新开发记录，不视为任务完成。

---

## 7. 开发进度状态定义

只使用以下状态：

| 状态 | 含义 |
|---|---|
| 待开始 | 已明确但尚未开发 |
| 进行中 | 正在开发，同一任务只能有一个主要负责人 |
| 阻塞 | 存在明确外部依赖或决策阻塞 |
| 待验证 | 代码已完成，但关键验证尚未完成 |
| 已完成 | 已满足验收标准并记录验证证据 |
| 已取消 | 明确不再实施，并记录原因 |

禁止把“代码写完但未测试”标记为已完成。

---

## 8. 文档同步规则

出现以下变化时必须同步文档：

| 变化 | 必须更新 |
|---|---|
| 产品范围、角色、流程或验收变化 | PRD |
| 模块边界、技术栈、目录、数据或安全变化 | 技术架构规范 |
| 重大技术决策 | `docs/adr` |
| API 契约变化 | OpenAPI 和 API 文档 |
| 数据库变化 | Migration 和数据字典 |
| 当前开发状态变化 | 开发进度与变更记录 |
| 部署或运维方式变化 | Runbook |

文档与代码不一致时，任务不得直接关闭。

---

## 9. AI 会话交接规范

每次 AI 开发会话结束前，必须确保下一位 Agent 可以从仓库恢复上下文。

交接至少包含：

- 当前任务状态。
- 已完成内容。
- 修改文件。
- 已运行验证。
- 未完成步骤。
- 阻塞原因。
- 下一步建议。

优先写入 `docs/开发进度与变更记录.md`，不能只保留在聊天记录中。

新会话不得仅依赖用户口述或历史聊天，应以仓库文档和代码为准。

### 9.1 会话中断恢复

Codex 遇到上下文压缩、线程切换或中断后，重新开始操作前必须再次读取：

1. `docs/当前任务.md`
2. `git status`
3. 当前任务涉及文件的 Diff
4. 最近 3 条提交

不得因为“记得之前做了什么”而跳过恢复检查。

### 9.2 Git 行为

- 未经用户明确要求，不执行强制推送、历史改写、删除分支和破坏性重置。
- 推送前检查远端历史和工作区状态。
- 提交信息描述单一目标。
- 不把密钥、Token、密码和个人凭证写入 Remote URL、Git Config、提交或文档。
- 用户要求完成并推送时，先验证再提交，提交后推送并报告 Commit。

---

## 10. 禁止事项

- 未读 PRD、架构规范和开发进度就开始修改代码。
- 重复实现已有功能。
- 未记录任务就进行大规模开发。
- 擅自改变产品范围或核心架构。
- 为赶进度绕过权限、数据隔离、测试或审计。
- 使用临时条件分支破坏插件扩展体系。
- 把所有业务堆入单个文件、Service 或页面。
- 声称测试通过但没有实际运行。
- 完成开发后不更新进度和变更记录。
- 仅在聊天中说明关键决策而不写入仓库。

---

## 11. 当前核心文档

- 项目入口：`README.md`
- 产品需求：`docs/Agent测试平台产品需求文档-PRD.md`
- 技术架构：`docs/Agent测试平台技术架构与开发规范.md`
- Codex 指南：`docs/Codex开发指南.md`
- 当前任务：`docs/当前任务.md`
- 开发进度：`docs/开发进度与变更记录.md`
- 实施计划：`docs/superpowers/plans/`
- 架构决策：`docs/adr/`
- 运维手册：`docs/runbooks/`

---

## 12. 子目录指令

当以下目录开始出现实际代码时，应在目录内添加精简的 `AGENTS.md`：

- `apps/web/AGENTS.md`：前端命令、组件规则和 UI 验证。
- `apps/control-api/AGENTS.md`：后端分层、迁移和测试命令。
- `workers/AGENTS.md`：Temporal、幂等、超时和隔离规则。
- `plugins/AGENTS.md`：Plugin SDK 边界和兼容性规则。

子目录指令只写该目录特有内容，不复制根目录全文。若需要临时替代同目录规则，使用 `AGENTS.override.md`，任务完成后删除。

