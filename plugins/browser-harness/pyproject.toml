[project]
name = "agenttest-plugin-browser"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "playwright>=1.56.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
