[project]
name = "agenttest-plugin-browser"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "browser-harness>=1.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
