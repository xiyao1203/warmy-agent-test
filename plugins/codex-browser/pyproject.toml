[project]
name = "agenttest-plugin-codex-browser"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "playwright>=1.50",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
